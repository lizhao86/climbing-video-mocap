# 账本工作流重做 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把攀岩账本的元数据录入从「网页表单」改成「Claude 对话追问」，网页退回纯只读的总览目录 + 详情页。

**Architecture:** 新增编排脚本 `climb_intake.py` 负责判重与跑流水线（不算指标）；元数据由 Claude 直接写 `素材/<片名>/线路.json`；`climb_journal.py` 聚合层扩展出难度双刻度荣誉榜与地点足迹；`climb_journal_card.py` 首页重做成三行并列 + 按月目录；删除网页填写服务。全程符合项目分层硬约定：指标层出 json、展示层只读 json、v1 脚本不动。

**Tech Stack:** Python 3.10（venv 在 `C:\venvs\climb310`，必须 ASCII 路径）、pytest、mediapipe 0.10.14、现有 climb_* 脚本链。

**设计依据：** [docs/superpowers/specs/2026-07-20-账本工作流重做-design.md](../specs/2026-07-20-账本工作流重做-design.md)

---

## 环境约定（每个任务都适用）

本项目在 Windows 上跑，有三条铁律（来龙去脉见 CLAUDE.md）：

- venv 必须在 ASCII 路径：`C:\venvs\climb310`（mediapipe 无法从含中文的路径加载模型）
- 跑流水线必须设 `PYTHONUTF8=1 PYTHONIOENCODING=utf-8 CLIMB_ROTATE=0`
- `CLIMB_ROTATE=0` 不加会让画面转两次，人躺着，但检出率仍 100%，结果全错

跑测试用：

```bash
cd "C:/Users/Administrator/Documents/我的代码/climbing-video-mocap"
PYTHONUTF8=1 PYTHONIOENCODING=utf-8 /c/venvs/climb310/Scripts/python.exe -m pytest tests/ -v
```

**写图不能用 `cv2.imwrite`**（非 ASCII 路径静默失败）。本计划不写图，但改到相关代码时注意。

## 文件结构

| 文件 | 责任 | 本计划的改动 |
|---|---|---|
| `climb_intake.py` | **新建**。编排层：算指纹、判重、跑流水线、落位。不算任何指标 | 全新 |
| `climb_journal.py` | 聚合层：扫 json + sidecar → `素材/journal.json` | 新增难度解析、荣誉榜、地点足迹；sidecar 字段改版 |
| `climb_journal_card.py` | 展示层：读 journal.json → `攀岩账本.html` | 首页三行并列 + 按月目录，删旧的 editable 分支 |
| `climb_report_card.py` | 展示层：读各 json → 单条报告卡 | 只加一条返回页眉，正文不动 |
| `climb_journal_edit.py` | 网页填写服务 | **删除** |
| `打开账本填写.bat` | 填写服务启动器 | **删除** |
| `tests/test_journal.py` | **新建**。难度解析与聚合的单测 | 全新 |
| `tests/test_intake.py` | **新建**。指纹与判重的单测 | 全新 |
| `.gitignore` / `README.md` / `CLAUDE.md` / `PLAN.md` | 文档 | 同步新流程 |

HTML 展示层不写自动化测试——本项目一贯靠人眼验收报告卡，保持一致。纯逻辑函数（难度解析、指纹、判重、聚合）走 TDD。

---

### Task 1: 测试基础设施

项目目前没有测试，先把地基铺好。

**Files:**
- Create: `tests/__init__.py`（空文件）
- Create: `tests/test_smoke.py`

- [ ] **Step 1: 装 pytest 到项目 venv**

```bash
/c/venvs/climb310/Scripts/python.exe -m pip install pytest
```

预期：`Successfully installed pytest-...`

- [ ] **Step 2: 建 tests 目录与空 __init__.py**

```bash
cd "C:/Users/Administrator/Documents/我的代码/climbing-video-mocap"
mkdir -p tests && touch tests/__init__.py
```

- [ ] **Step 3: 写一个冒烟测试，确认能 import 项目脚本**

`tests/test_smoke.py`：

```python
# -*- coding: utf-8 -*-
"""冒烟测试：确认测试能从项目根 import 脚本模块。"""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))


def test_can_import_journal():
    import climb_journal
    assert hasattr(climb_journal, "aggregate")
```

- [ ] **Step 4: 跑测试确认通过**

```bash
PYTHONUTF8=1 PYTHONIOENCODING=utf-8 /c/venvs/climb310/Scripts/python.exe -m pytest tests/ -v
```

预期：`1 passed`

- [ ] **Step 5: 提交**

```bash
git add tests/
git commit -m "test: 铺测试地基，pytest + 冒烟用例"
```

---

### Task 2: 难度双刻度解析

抱石用 V 级、绳索用 5.x（YDS），两套刻度不能混排。这个函数放在聚合层 `climb_journal.py`——展示层只读算好的结果，不自己解析（分层硬约定）。

经典陷阱：**5.9 比 5.10a 简单**，字符串排序会搞反。

**Files:**
- Create: `tests/test_journal.py`
- Modify: `climb_journal.py`（在 `count_moves` 之前加函数）

- [ ] **Step 1: 写失败的测试**

`tests/test_journal.py`：

```python
# -*- coding: utf-8 -*-
"""聚合层单测：难度解析、荣誉榜、地点足迹。"""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from climb_journal import parse_grade


def test_v_scale():
    assert parse_grade("V4") == ("V", 4)
    assert parse_grade("v10") == ("V", 10)
    assert parse_grade(" V0 ") == ("V", 0)


def test_yds_scale():
    assert parse_grade("5.10b")[0] == "YDS"
    assert parse_grade("5.12A")[0] == "YDS"


def test_yds_ordering_5_9_easier_than_5_10a():
    """字符串排序会把 5.9 排在 5.10a 后面，rank 必须排对。"""
    _, r59 = parse_grade("5.9")
    _, r10a = parse_grade("5.10a")
    _, r10b = parse_grade("5.10b")
    assert r59 < r10a < r10b


def test_unrecognized():
    assert parse_grade("") == (None, None)
    assert parse_grade(None) == (None, None)
    assert parse_grade("紫色") == (None, None)
    assert parse_grade("V4/V5") == (None, None)
```

- [ ] **Step 2: 跑测试确认失败**

```bash
PYTHONUTF8=1 PYTHONIOENCODING=utf-8 /c/venvs/climb310/Scripts/python.exe -m pytest tests/test_journal.py -v
```

预期：FAIL，`ImportError: cannot import name 'parse_grade'`

- [ ] **Step 3: 实现 parse_grade**

在 `climb_journal.py` 的 `count_moves` 函数**之前**插入：

```python
# ---------------- 难度双刻度 ----------------
# 抱石 V 级与绳索 YDS(5.x) 是两套刻度，不能混一个榜。
# rank 只用于同刻度内排序，跨刻度比较无意义。
GRADE_V = re.compile(r"^[Vv](\d+)$")
GRADE_YDS = re.compile(r"^5\.(\d+)([a-dA-D])?$")


def parse_grade(grade):
    """难度字符串 → (scale, rank)。无法识别返回 (None, None)。

    scale ∈ {"V", "YDS"}。rank 是同刻度内的序号，越大越难。
    YDS 用 数字*4+字母 编码，保证 5.9(36) < 5.10a(40) < 5.10b(41)——
    字符串排序会把 5.9 排到 5.10a 之后，必须走这里。
    """
    g = (grade or "").strip()
    m = GRADE_V.match(g)
    if m:
        return "V", int(m.group(1))
    m = GRADE_YDS.match(g)
    if m:
        letter = (m.group(2) or "a").lower()
        return "YDS", int(m.group(1)) * 4 + (ord(letter) - ord("a"))
    return None, None
```

- [ ] **Step 4: 跑测试确认通过**

```bash
PYTHONUTF8=1 PYTHONIOENCODING=utf-8 /c/venvs/climb310/Scripts/python.exe -m pytest tests/test_journal.py -v
```

预期：`4 passed`

- [ ] **Step 5: 提交**

```bash
git add tests/test_journal.py climb_journal.py
git commit -m "feat(journal): 难度双刻度解析，5.9 与 5.10a 排序修正"
```

---

### Task 3: sidecar 字段改版

`线路.json` 加「类型」、把「岩馆」改叫「地点」、加「视频指纹」和「登记时间」。现有 7 个 sidecar 全是空的，改字段名零成本。

`collect_entry` 输出的 entry 里 `gym` 改叫 `place`，并新增 `type` / `grade_scale` / `grade_rank` / `fingerprint`。

⚠️ `climb_journal_card.py:107` 附近的 `_row_head` 用了 `e["gym"]`，本任务必须一并改，否则账本页会崩。

**Files:**
- Modify: `climb_journal.py`（`SIDECAR_TEMPLATE`、`CONFIG_TEMPLATE`、`collect_entry`）
- Modify: `climb_journal_card.py`（`_row_head` 里的 `gym` → `place`）
- Modify: `素材/*/线路.json`（7 份，跑 `--init` 重生成）

- [ ] **Step 1: 改模板常量**

`climb_journal.py` 里把 `SIDECAR_TEMPLATE` 和 `CONFIG_TEMPLATE` 整段替换为：

```python
SIDECAR_TEMPLATE = {
    "类型": "",          # 抱石 / 顶绳 / 先锋
    "难度": "",          # 抱石填 V4，绳索填 5.10b
    "完攀": None,
    "地点": "",          # 岩馆名或野外岩场名
    "线路名": "",        # 野外才有；室内线路通常没名字
    "日期": "",
    "备注": "",
    "视频指纹": "",      # climb_intake.py 自动填，判重用
    "登记时间": "",
}
CONFIG_TEMPLATE = {"身高_m": None, "体重_kg": None}
```

- [ ] **Step 2: 改 collect_entry 的返回字段**

`climb_journal.py` 的 `collect_entry` 里，把 `return {...}` 中这三行：

```python
        "route_name": (sidecar.get("线路名") or "").strip(),
        "gym": (sidecar.get("岩馆") or "").strip(),
        "grade": (sidecar.get("难度") or "").strip(),
```

替换为：

```python
        "route_name": (sidecar.get("线路名") or "").strip(),
        "place": (sidecar.get("地点") or "").strip(),
        "type": (sidecar.get("类型") or "").strip(),
        "grade": grade,
        "grade_scale": grade_scale,
        "grade_rank": grade_rank,
        "fingerprint": (sidecar.get("视频指纹") or "").strip(),
```

并在同一函数里、`comp = mv2.get("completion", {})` 这一行**之前**插入：

```python
    grade = (sidecar.get("难度") or "").strip()
    grade_scale, grade_rank = parse_grade(grade)
```

- [ ] **Step 3: 改展示层的 gym 引用**

`climb_journal_card.py` 的 `_row_head` 函数里，把读 `gym` 的那一行改为读 `place`：

```python
    gym = esc(e.get("place") or "")
```

（变量名 `gym` 保持不动，只改取值的键——这一处 Task 8 会整体重写，先保证不崩。）

- [ ] **Step 4: 重新生成 7 份 sidecar 模板**

旧模板字段名已变，先删再生成。**这 7 份现在全是空值，删掉不丢任何数据**——先确认这一点：

```bash
cd "C:/Users/Administrator/Documents/我的代码/climbing-video-mocap"
cat 素材/*/线路.json
```

预期：每份的「线路名/岩馆/难度/完攀/日期/备注」全是 `""` 或 `null`。若**有任何一份填了内容，停下来报告**，不要删。

确认全空后：

```bash
rm 素材/*/线路.json
PYTHONUTF8=1 PYTHONIOENCODING=utf-8 /c/venvs/climb310/Scripts/python.exe climb_journal.py --init
cat 素材/IMG_6152/线路.json
```

预期：新模板，含「类型」「地点」「视频指纹」等字段。

- [ ] **Step 5: 跑聚合确认不崩**

```bash
PYTHONUTF8=1 PYTHONIOENCODING=utf-8 /c/venvs/climb310/Scripts/python.exe climb_journal.py
PYTHONUTF8=1 PYTHONIOENCODING=utf-8 /c/venvs/climb310/Scripts/python.exe climb_journal_card.py
```

预期：`journal.json: 7 条记录`，账本页正常生成。

- [ ] **Step 6: 提交**

```bash
git add climb_journal.py climb_journal_card.py 素材/*/线路.json 账本配置.json
git commit -m "feat(journal): sidecar 字段改版，岩馆改地点，新增类型与视频指纹"
```

---

### Task 4: 荣誉榜与地点足迹聚合

首页三行要的数字：荣誉行（双刻度最高难度 + 各难度完攀数）、足迹行（去过几个地点）。

**Files:**
- Modify: `tests/test_journal.py`（追加）
- Modify: `climb_journal.py`（`aggregate` 函数）

- [ ] **Step 1: 写失败的测试**

在 `tests/test_journal.py` 末尾追加：

```python
from climb_journal import honors, places


def _entry(grade, sent, place="", scale_from_grade=True):
    """构造一条最小 entry，只带 honors/places 关心的字段。"""
    scale, rank = parse_grade(grade) if scale_from_grade else (None, None)
    return {"grade": grade, "grade_scale": scale, "grade_rank": rank,
            "sent": sent, "place": place}


def test_honors_splits_two_scales():
    es = [_entry("V4", True), _entry("V5", True), _entry("5.10b", True)]
    h = honors(es)
    assert h["V"]["best_sent"] == "V5"
    assert h["YDS"]["best_sent"] == "5.10b"
    assert h["V"]["n_sent"] == 2


def test_honors_best_is_hardest_not_latest():
    """最高难度看 rank，不看出现顺序。"""
    es = [_entry("V7", True), _entry("V3", True)]
    assert honors(es)["V"]["best_sent"] == "V7"


def test_honors_unsent_counts_as_attempt_only():
    es = [_entry("V6", False), _entry("V4", True)]
    h = honors(es)
    assert h["V"]["best_sent"] == "V4"      # V6 没送顶，不进荣誉
    assert h["V"]["n_attempt"] == 2
    assert h["V"]["n_sent"] == 1
    assert h["V"]["by_grade"] == {"V4": 1}


def test_honors_yds_ordering():
    es = [_entry("5.9", True), _entry("5.10a", True)]
    assert honors(es)["YDS"]["best_sent"] == "5.10a"


def test_honors_skips_unrecognized_grade():
    es = [_entry("紫色", True), _entry("V4", True)]
    h = honors(es)
    assert list(h.keys()) == ["V"]


def test_places_dedupes_and_ignores_blank():
    es = [_entry("V4", True, "岩时"), _entry("V4", True, "岩时"),
          _entry("V4", True, "六盘水"), _entry("V4", True, "")]
    p = places(es)
    assert p["n"] == 2
    assert p["list"] == ["六盘水", "岩时"]
```

- [ ] **Step 2: 跑测试确认失败**

```bash
PYTHONUTF8=1 PYTHONIOENCODING=utf-8 /c/venvs/climb310/Scripts/python.exe -m pytest tests/test_journal.py -v
```

预期：FAIL，`ImportError: cannot import name 'honors'`

- [ ] **Step 3: 实现 honors 和 places**

在 `climb_journal.py` 的 `def aggregate(...)` **之前**插入：

```python
def honors(entries):
    """→ {scale: {best_sent, best_sent_rank, by_grade, n_sent, n_attempt}}。

    完攀的才进 best_sent 和 by_grade；没送顶的只算 n_attempt。
    难度识别不出来的条目整条跳过（它仍进里程/足迹统计）。
    """
    out = {}
    for e in entries:
        scale, rank = e.get("grade_scale"), e.get("grade_rank")
        if not scale:
            continue
        h = out.setdefault(scale, {"best_sent": None, "best_sent_rank": -1,
                                   "by_grade": {}, "n_sent": 0, "n_attempt": 0})
        h["n_attempt"] += 1
        if e.get("sent") is True:
            h["n_sent"] += 1
            g = e["grade"]
            h["by_grade"][g] = h["by_grade"].get(g, 0) + 1
            if rank > h["best_sent_rank"]:
                h["best_sent_rank"] = rank
                h["best_sent"] = g
    return out


def places(entries):
    """→ {n, list}。去重、去空、按名排序。"""
    ps = sorted({(e.get("place") or "").strip()
                 for e in entries if (e.get("place") or "").strip()})
    return {"n": len(ps), "list": ps}


def n_unknown_grade(entries):
    """填了难度但识别不出来的条数——账本页要如实标出来，不能装作没有。"""
    return sum(1 for e in entries
               if (e.get("grade") or "").strip() and not e.get("grade_scale"))
```

- [ ] **Step 4: 接进 aggregate**

在 `aggregate` 函数里，把 `totals.update({...})` 那个字典中的这一行：

```python
        "sent_by_grade": grades(entries),
```

替换为：

```python
        "honors": honors(entries),
        "places": places(entries),
        "n_unknown_grade": n_unknown_grade(entries),
        "sent_by_grade": grades(entries),   # 旧字段，Task 8 改完展示层后删
```

并把 `tm["sent_by_grade"] = grades(month_entries)` 这一行下面加一行：

```python
    tm["honors"] = honors(month_entries)
```

- [ ] **Step 5: 跑测试确认通过**

```bash
PYTHONUTF8=1 PYTHONIOENCODING=utf-8 /c/venvs/climb310/Scripts/python.exe -m pytest tests/test_journal.py -v
```

预期：`10 passed`

- [ ] **Step 6: 跑真实聚合，确认 honors 字段落进 journal.json**

```bash
PYTHONUTF8=1 PYTHONIOENCODING=utf-8 /c/venvs/climb310/Scripts/python.exe climb_journal.py
PYTHONUTF8=1 PYTHONIOENCODING=utf-8 /c/venvs/climb310/Scripts/python.exe -c "import json;d=json.load(open('素材/journal.json',encoding='utf-8'));print(d['totals']['honors'], d['totals']['places'])"
```

预期：`{} {'n': 0, 'list': []}`（7 条素材元数据还是空的，Task 10 才补）

- [ ] **Step 7: 提交**

```bash
git add tests/test_journal.py climb_journal.py
git commit -m "feat(journal): 双刻度荣誉榜与地点足迹聚合"
```

---

### Task 5: 视频指纹与判重

指纹 = `sha256(文件大小 + 文件头 8MB)`。取头部而非全文是因为原片可达数百 MB，全文哈希拖慢扫描；改文件名认得出，重新编码认不出（可接受，那本就是另一个文件）。

**Files:**
- Create: `climb_intake.py`
- Create: `tests/test_intake.py`

- [ ] **Step 1: 写失败的测试**

`tests/test_intake.py`：

```python
# -*- coding: utf-8 -*-
"""编排层单测：指纹与判重。"""
import json
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from climb_intake import video_fingerprint, build_fingerprint_index


def _write(path, data):
    with open(path, "wb") as f:
        f.write(data)


def test_fingerprint_stable(tmp_path):
    p = tmp_path / "a.mov"
    _write(p, b"climbing" * 100)
    assert video_fingerprint(str(p)) == video_fingerprint(str(p))


def test_fingerprint_survives_rename(tmp_path):
    """改文件名要认得出是同一个视频。"""
    a, b = tmp_path / "a.mov", tmp_path / "b.mov"
    _write(a, b"climbing" * 100)
    _write(b, b"climbing" * 100)
    assert video_fingerprint(str(a)) == video_fingerprint(str(b))


def test_fingerprint_differs_on_content(tmp_path):
    a, b = tmp_path / "a.mov", tmp_path / "b.mov"
    _write(a, b"climbing" * 100)
    _write(b, b"bouldering" * 100)
    assert video_fingerprint(str(a)) != video_fingerprint(str(b))


def test_fingerprint_differs_on_size_alone(tmp_path):
    """头部相同但长度不同（截断版）必须区分开。"""
    a, b = tmp_path / "a.mov", tmp_path / "b.mov"
    _write(a, b"x" * 1000)
    _write(b, b"x" * 2000)
    assert video_fingerprint(str(a)) != video_fingerprint(str(b))


def test_fingerprint_prefix(tmp_path):
    p = tmp_path / "a.mov"
    _write(p, b"x" * 10)
    assert video_fingerprint(str(p)).startswith("sha256:")


def test_index_maps_fingerprint_to_folder(tmp_path):
    mdir = tmp_path / "素材"
    for base, fp in [("IMG_1", "sha256:aaa"), ("IMG_2", "sha256:bbb")]:
        d = mdir / base
        d.mkdir(parents=True)
        with open(d / "线路.json", "w", encoding="utf-8") as f:
            json.dump({"视频指纹": fp, "难度": "V4"}, f, ensure_ascii=False)
    idx = build_fingerprint_index(str(mdir))
    assert idx["sha256:aaa"]["base"] == "IMG_1"
    assert idx["sha256:bbb"]["sidecar"]["难度"] == "V4"


def test_index_ignores_blank_fingerprint(tmp_path):
    """指纹为空的老条目不能占位，否则新视频会被误判成重复。"""
    mdir = tmp_path / "素材"
    d = mdir / "IMG_1"
    d.mkdir(parents=True)
    with open(d / "线路.json", "w", encoding="utf-8") as f:
        json.dump({"视频指纹": "", "难度": ""}, f, ensure_ascii=False)
    assert build_fingerprint_index(str(mdir)) == {}
```

- [ ] **Step 2: 跑测试确认失败**

```bash
PYTHONUTF8=1 PYTHONIOENCODING=utf-8 /c/venvs/climb310/Scripts/python.exe -m pytest tests/test_intake.py -v
```

预期：FAIL，`ModuleNotFoundError: No module named 'climb_intake'`

- [ ] **Step 3: 建 climb_intake.py，先只写指纹与索引**

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
climb_intake.py —— 收件箱编排层（2026-07-20）

职责只有三件：算指纹判重、跑流水线、把产物落位到 素材/<片名>/。
**不算任何指标**——指标归 climb_analyze_report / climb_segments / climb_report_v2，
展示归 climb_report_card / climb_journal_card（分层硬约定见 CLAUDE.md）。

元数据（类型/难度/完攀/地点/线路名）不由本脚本采集——它由 Claude 在对话里
追问后直接写 素材/<片名>/线路.json。网页表单已于本次改版删除（7 条素材填了
零条，证伪了「让老板去网页填」）。

用法:
    python climb_intake.py scan                 # 列收件箱视频 + 判重结果（JSON）
    python climb_intake.py run <视频路径>        # 跑完整流水线并落位
    python climb_intake.py run <视频> --roi --seed X,Y [--seed-roi W,H] [--start-s S]
                                                # 远景素材走 ROI 取数

设计定案见 docs/superpowers/specs/2026-07-20-账本工作流重做-design.md。
"""
import hashlib
import json
import os
import sys

INBOX_DIR = "收件箱"
MATERIAL_DIR = "素材"
SIDECAR_NAME = "线路.json"
VIDEO_EXTS = (".mov", ".mp4")
HEAD_BYTES = 8 * 1024 * 1024      # 只哈希文件头，原片可达数百 MB

ROOT = os.path.dirname(os.path.abspath(__file__))


def video_fingerprint(path, head_bytes=HEAD_BYTES):
    """→ "sha256:<32 位十六进制>"。

    哈希 = 文件大小 + 文件头 head_bytes。改文件名认得出是同一个视频；
    重新编码或裁剪认不出（那本来就是另一个文件，可接受）。
    大小必须入哈希，否则截断版与原版会撞。
    """
    h = hashlib.sha256()
    h.update(str(os.path.getsize(path)).encode())
    with open(path, "rb") as f:
        h.update(f.read(head_bytes))
    return "sha256:" + h.hexdigest()[:32]


def build_fingerprint_index(material_dir):
    """扫 <素材>/*/线路.json → {指纹: {base, dir, sidecar}}。

    指纹为空的条目跳过——否则空串会占位，让新视频全被误判成重复。
    """
    idx = {}
    if not os.path.isdir(material_dir):
        return idx
    for name in sorted(os.listdir(material_dir)):
        folder = os.path.join(material_dir, name)
        sc_path = os.path.join(folder, SIDECAR_NAME)
        if not os.path.isdir(folder) or not os.path.exists(sc_path):
            continue
        try:
            with open(sc_path, encoding="utf-8") as f:
                sc = json.load(f)
        except (ValueError, OSError):
            continue
        fp = (sc.get("视频指纹") or "").strip()
        if not fp:
            continue
        idx[fp] = {"base": name, "dir": folder, "sidecar": sc}
    return idx
```

- [ ] **Step 4: 跑测试确认通过**

```bash
PYTHONUTF8=1 PYTHONIOENCODING=utf-8 /c/venvs/climb310/Scripts/python.exe -m pytest tests/test_intake.py -v
```

预期：`7 passed`

- [ ] **Step 5: 提交**

```bash
git add climb_intake.py tests/test_intake.py
git commit -m "feat(intake): 视频指纹与判重索引"
```

---

### Task 6: scan 子命令

输出结构化 JSON 给 Claude 读，让它知道有哪些新视频、哪些是重复的。

**Files:**
- Modify: `climb_intake.py`（追加）
- Modify: `tests/test_intake.py`（追加）
- Create: `收件箱/.gitkeep`

- [ ] **Step 1: 写失败的测试**

在 `tests/test_intake.py` 末尾追加：

```python
from climb_intake import scan_inbox


def test_scan_flags_new_and_duplicate(tmp_path):
    inbox = tmp_path / "收件箱"
    inbox.mkdir()
    mdir = tmp_path / "素材"

    # 已入库的一条，指纹取自它的原片
    old_dir = mdir / "IMG_OLD"
    old_dir.mkdir(parents=True)
    _write(old_dir / "IMG_OLD.MOV", b"same content" * 50)
    fp = video_fingerprint(str(old_dir / "IMG_OLD.MOV"))
    with open(old_dir / "线路.json", "w", encoding="utf-8") as f:
        json.dump({"视频指纹": fp, "难度": "V4", "完攀": True,
                   "地点": "岩时", "日期": "2026-03-29"}, f, ensure_ascii=False)

    # 收件箱：一条重复（改了名）、一条全新
    _write(inbox / "renamed.MOV", b"same content" * 50)
    _write(inbox / "IMG_NEW.MOV", b"brand new" * 50)

    got = scan_inbox(str(inbox), str(mdir))
    by_name = {item["base"]: item for item in got}

    assert by_name["renamed"]["duplicate_of"] == "IMG_OLD"
    assert by_name["renamed"]["dup_sidecar"]["难度"] == "V4"
    assert by_name["IMG_NEW"]["duplicate_of"] is None


def test_scan_ignores_non_video(tmp_path):
    inbox = tmp_path / "收件箱"
    inbox.mkdir()
    _write(inbox / "readme.txt", b"hello")
    _write(inbox / ".gitkeep", b"")
    assert scan_inbox(str(inbox), str(tmp_path / "素材")) == []


def test_scan_empty_inbox(tmp_path):
    inbox = tmp_path / "收件箱"
    inbox.mkdir()
    assert scan_inbox(str(inbox), str(tmp_path / "素材")) == []
```

- [ ] **Step 2: 跑测试确认失败**

```bash
PYTHONUTF8=1 PYTHONIOENCODING=utf-8 /c/venvs/climb310/Scripts/python.exe -m pytest tests/test_intake.py -v
```

预期：FAIL，`ImportError: cannot import name 'scan_inbox'`

- [ ] **Step 3: 实现 scan_inbox**

在 `climb_intake.py` 的 `build_fingerprint_index` 之后追加：

```python
def scan_inbox(inbox_dir, material_dir):
    """→ [{file, base, size_mb, fingerprint, duplicate_of, dup_sidecar}]。

    duplicate_of 是重复命中的素材文件夹名，没命中为 None。
    """
    idx = build_fingerprint_index(material_dir)
    out = []
    if not os.path.isdir(inbox_dir):
        return out
    for name in sorted(os.listdir(inbox_dir)):
        path = os.path.join(inbox_dir, name)
        if not os.path.isfile(path):
            continue
        if os.path.splitext(name)[1].lower() not in VIDEO_EXTS:
            continue
        fp = video_fingerprint(path)
        hit = idx.get(fp)
        out.append({
            "file": path,
            "base": os.path.splitext(name)[0],
            "size_mb": round(os.path.getsize(path) / 1024 / 1024, 1),
            "fingerprint": fp,
            "duplicate_of": hit["base"] if hit else None,
            "dup_sidecar": hit["sidecar"] if hit else None,
        })
    return out
```

- [ ] **Step 4: 加 main 入口**

在 `climb_intake.py` 末尾追加：

```python
def cmd_scan():
    items = scan_inbox(os.path.join(ROOT, INBOX_DIR),
                       os.path.join(ROOT, MATERIAL_DIR))
    print(json.dumps({"inbox": items}, ensure_ascii=False, indent=2))


def main():
    if len(sys.argv) < 2 or sys.argv[1] not in ("scan", "run"):
        print(__doc__)
        sys.exit(1)
    if sys.argv[1] == "scan":
        cmd_scan()


if __name__ == "__main__":
    main()
```

- [ ] **Step 5: 建收件箱目录并跑一次**

```bash
cd "C:/Users/Administrator/Documents/我的代码/climbing-video-mocap"
mkdir -p 收件箱 && touch 收件箱/.gitkeep
PYTHONUTF8=1 PYTHONIOENCODING=utf-8 /c/venvs/climb310/Scripts/python.exe climb_intake.py scan
```

预期：`{"inbox": []}`（收件箱是空的）

- [ ] **Step 6: 跑测试确认通过**

```bash
PYTHONUTF8=1 PYTHONIOENCODING=utf-8 /c/venvs/climb310/Scripts/python.exe -m pytest tests/ -v
```

预期：`11 passed`（intake 10 + smoke 1）

- [ ] **Step 7: gitignore 收件箱内容但保留目录**

在 `.gitignore` 的「大文件不入库」段之后加：

```
# 收件箱：临时投放区，视频不入 git，目录本身保留
收件箱/*
!收件箱/.gitkeep
```

- [ ] **Step 8: 提交**

```bash
git add climb_intake.py tests/test_intake.py .gitignore 收件箱/.gitkeep
git commit -m "feat(intake): scan 子命令，列收件箱并判重"
```

---

### Task 7: run 子命令

跑完整流水线并落位。三条 Windows 铁律写死在脚本里，老板和 Claude 都不用再记。

流水线六步（命令出处见 README「分析一条新线」段）：

1. `climb_pose.py` → 3 份 CSV + 标注视频
2. `climb_analyze_report.py` → metrics.json（v1，`--out` 指到 `数据/v1原始/` 让它的旧报告卡写到角落）
3. `climb_segments.py` → segments.json
4. `climb_report_v2.py` → metrics_v2.json
5. `climb_match.py` → recognition.json（动作收集要它）
6. `climb_report_card.py` → 攀岩报告卡.html

**Files:**
- Modify: `climb_intake.py`（追加）

- [ ] **Step 1: 加环境与子进程工具**

在 `climb_intake.py` 的 `import` 段补 `import re`、`import shutil`、`import subprocess`，
并在 `ROOT = ...` 之后追加：

```python
# Windows 三条铁律（来龙去脉见 CLAUDE.md）：
#   CLIMB_ROTATE=0 —— OpenCV 在 Windows 会自动应用旋转元数据，脚本再转一次
#     → 人躺着，但检出率仍 100%，重心高度量成水平方向，结果全错。
#   PYTHONUTF8 / PYTHONIOENCODING —— v1 脚本 open() 没指定编码，GBK 写不了 ▸。
#   venv 必须 ASCII 路径 —— mediapipe 无法从含中文的路径加载模型。
PIPELINE_ENV = {
    "PYTHONUTF8": "1",
    "PYTHONIOENCODING": "utf-8",
    "CLIMB_MAX_W": "960",
    "CLIMB_ROTATE": "0",
}
DEFAULT_PY = r"C:\venvs\climb310\Scripts\python.exe"
DETECT_RATE_MIN = 50      # 低于这个百分比提示改走 ROI 流程


def _python():
    return os.environ.get("CLIMB_PY", DEFAULT_PY)


def run_step(args, label):
    """跑一条流水线命令，实时透传输出。→ (成功?, 完整 stdout)"""
    env = dict(os.environ)
    env.update(PIPELINE_ENV)
    print("\n=== %s ===" % label, flush=True)
    proc = subprocess.Popen([_python()] + args, cwd=ROOT, env=env,
                            stdout=subprocess.PIPE, stderr=subprocess.STDOUT,
                            text=True, encoding="utf-8", errors="replace")
    lines = []
    for line in proc.stdout:
        print(line, end="", flush=True)
        lines.append(line)
    proc.wait()
    return proc.returncode == 0, "".join(lines)
```

- [ ] **Step 2: 实现 run_pipeline**

在 `climb_intake.py` 的 `cmd_scan` **之前**追加：

```python
def _detect_rate(pose_stdout):
    """从 climb_pose 的输出里抓「检测到人体的帧: 123/456 (78%)」→ 78，抓不到返回 None。"""
    m = re.search(r"检测到人体的帧:\s*\d+/\d+\s*\((\d+)%\)", pose_stdout)
    return int(m.group(1)) if m else None


def run_pipeline(video_path, roi_args=None):
    """跑完整流水线并落位。→ {base, dir, ok, detect_rate, warnings}"""
    base = os.path.splitext(os.path.basename(video_path))[0]
    dest = os.path.join(ROOT, MATERIAL_DIR, base)
    data = os.path.join(dest, "数据")
    for d in (data, os.path.join(dest, "报告卡素材"), os.path.join(dest, "标注视频"),
              os.path.join(data, "v1原始")):
        os.makedirs(d, exist_ok=True)

    # 视频不在素材文件夹里就搬进去；已经在了（重跑场景）就原地用
    target_video = os.path.join(dest, os.path.basename(video_path))
    if os.path.abspath(video_path) != os.path.abspath(target_video):
        shutil.move(video_path, target_video)
    rel_video = os.path.relpath(target_video, ROOT)
    rel_data = os.path.relpath(data, ROOT)
    rel_dest = os.path.relpath(dest, ROOT)
    rel_v1 = os.path.relpath(os.path.join(data, "v1原始"), ROOT)

    warnings = []

    if roi_args:
        ok, out = run_step(["climb_pose_roi.py", rel_video, rel_data] + roi_args,
                           "1/6 骨架提取（ROI 远景版）")
    else:
        ok, out = run_step(["climb_pose.py", rel_video, rel_data], "1/6 骨架提取")
    if not ok:
        return {"base": base, "dir": rel_dest, "ok": False,
                "detect_rate": None, "warnings": ["骨架提取失败，后续步骤未执行"]}

    rate = _detect_rate(out)
    if rate is not None and rate < DETECT_RATE_MIN and not roi_args:
        warnings.append(
            "检出率只有 %d%%（低于 %d%%）。人在画面里可能太小，"
            "建议改走 ROI 流程重跑：run <视频> --roi --seed X,Y" % (rate, DETECT_RATE_MIN))

    steps = [
        (["climb_analyze_report.py", "--dir", rel_data, "--base", base,
          "--annotated", os.path.join(rel_data, base + "_annotated.mp4"),
          "--out", rel_v1], "2/6 v1 指标"),
        (["climb_segments.py", os.path.join(rel_data, base + "_pose2d.csv")], "3/6 动作分段"),
        (["climb_report_v2.py", "--dir", rel_data, "--base", base, "--out", rel_v1], "4/6 节奏指标"),
        (["climb_match.py", os.path.join(rel_data, base + "_segments.json")], "5/6 动作识别"),
        (["climb_report_card.py", "--dir", rel_data, "--base", base,
          "--video", rel_video, "--out", rel_dest], "6/6 报告卡"),
    ]
    for args, label in steps:
        ok, _ = run_step(args, label)
        if not ok:
            return {"base": base, "dir": rel_dest, "ok": False,
                    "detect_rate": rate, "warnings": warnings + ["%s 失败" % label]}

    # sidecar：不存在就建模板并写指纹；已存在（重跑）就只补指纹，不动老板填的内容
    sc_path = os.path.join(dest, SIDECAR_NAME)
    sc = {}
    if os.path.exists(sc_path):
        try:
            with open(sc_path, encoding="utf-8") as f:
                sc = json.load(f)
        except (ValueError, OSError):
            sc = {}
    for k, v in SIDECAR_TEMPLATE.items():
        sc.setdefault(k, v)
    sc["视频指纹"] = video_fingerprint(target_video)
    with open(sc_path, "w", encoding="utf-8") as f:
        json.dump(sc, f, ensure_ascii=False, indent=2)

    warnings.append("检出率高不代表跟对了人。开 标注视频/ 里的标注视频抽看几帧，"
                    "确认骨架一直在攀岩者身上（有路人走过会跟错，检出率不会掉）。")
    return {"base": base, "dir": rel_dest, "ok": True,
            "detect_rate": rate, "warnings": warnings}
```

`SIDECAR_TEMPLATE` 需要从聚合层导入——在 `climb_intake.py` 的 import 段末尾加：

```python
from climb_journal import SIDECAR_TEMPLATE
```

- [ ] **Step 3: 加 run 的命令行解析**

把 `climb_intake.py` 的 `main()` 整个替换为：

```python
def cmd_run(argv):
    video = argv[0]
    if not os.path.exists(video):
        print("找不到视频: %s" % video)
        sys.exit(1)
    roi_args = None
    if "--roi" in argv:
        roi_args = []
        for flag in ("--seed", "--seed-roi", "--start-s"):
            if flag in argv:
                roi_args += [flag, argv[argv.index(flag) + 1]]
        if "--seed" not in argv:
            print("--roi 必须同时给 --seed X,Y（首帧攀岩者在全画幅的像素坐标）")
            sys.exit(1)
    result = run_pipeline(video, roi_args)
    print("\n" + json.dumps(result, ensure_ascii=False, indent=2))
    sys.exit(0 if result["ok"] else 1)


def main():
    if len(sys.argv) < 2 or sys.argv[1] not in ("scan", "run"):
        print(__doc__)
        sys.exit(1)
    if sys.argv[1] == "scan":
        cmd_scan()
    else:
        if len(sys.argv) < 3:
            print("用法: python climb_intake.py run <视频路径>")
            sys.exit(1)
        cmd_run(sys.argv[2:])
```

- [ ] **Step 4: 用一条已有素材实跑验证**

拿 IMG_6152 重跑（它已在素材库里，`run` 会原地用不搬动）：

```bash
cd "C:/Users/Administrator/Documents/我的代码/climbing-video-mocap"
PYTHONUTF8=1 PYTHONIOENCODING=utf-8 /c/venvs/climb310/Scripts/python.exe \
    climb_intake.py run 素材/IMG_6152/IMG_6152.MOV
```

预期：六步依次跑完，末尾 JSON 里 `"ok": true`，`detect_rate` 在 90 以上，
`素材/IMG_6152/线路.json` 的「视频指纹」被填上。31s 的视频大约要跑几分钟。

⚠️ 若报 `FileNotFoundError: pose_landmark_cpu.binarypb`，是 venv 路径含中文——
检查 `CLIMB_PY` 环境变量有没有被设成别的 Python。

- [ ] **Step 5: 确认判重现在能命中**

```bash
PYTHONUTF8=1 PYTHONIOENCODING=utf-8 /c/venvs/climb310/Scripts/python.exe -c "
import climb_intake as ci
idx = ci.build_fingerprint_index('素材')
print(len(idx), list(idx.values())[0]['base'] if idx else '空')
"
```

预期：`1 IMG_6152`

- [ ] **Step 6: 跑全部测试**

```bash
PYTHONUTF8=1 PYTHONIOENCODING=utf-8 /c/venvs/climb310/Scripts/python.exe -m pytest tests/ -v
```

预期：`11 passed`

- [ ] **Step 7: 提交**

```bash
git add climb_intake.py 素材/IMG_6152/线路.json
git commit -m "feat(intake): run 子命令，六步流水线 + 三条铁律固化"
```

---

### Task 8: 账本首页重做

三行并列（荣誉 / 里程 / 足迹）+ 按月分组的目录。删掉 editable 分支——填写服务这次要删。

**Files:**
- Modify: `climb_journal_card.py`
- Modify: `climb_journal.py`（删 `sent_by_grade` 旧字段）

- [ ] **Step 1: 删展示层的 editable 分支**

`climb_journal_card.py` 里删掉这些（它们只服务于已废弃的填写服务）：

- `entry_row_edit` 整个函数
- `build_page(j, editable=False)` 的 `editable` 形参，及函数体内所有 `if editable:` 分支
- `<div class="savebar">` 那段 HTML 与配套 CSS/JS
- 两处 `hint` 文案里提到「打开账本填写.bat」和「行内直接填」的分支，统一改成：

```python
    hint = ('<p class="hint">难度、地点这些信息在对话里跟 Claude 说一句就记上了，'
            '不用在这个页面填。</p>')
```

改完 `build_page` 签名是 `def build_page(j):`，`main()` 里的调用同步去掉参数。

- [ ] **Step 2: 三行并列的行渲染函数**

在 `climb_journal_card.py` 的 `def build_page` **之前**加：

```python
def honor_row(j):
    """荣誉行：双刻度最高难度 + 各难度完攀数分布 + 本月最难。"""
    h = j["totals"].get("honors") or {}
    hm = j.get("this_month", {}).get("honors") or {}
    cells = []
    for scale, label in (("V", "抱石最高"), ("YDS", "绳索最高")):
        s = h.get(scale)
        cells.append(kpi(s["best_sent"] if s and s["best_sent"] else "—", "", label))
    month_best = [s["best_sent"] for s in hm.values() if s.get("best_sent")]
    cells.append(kpi(" / ".join(month_best) if month_best else "—", "", "本月最难"))

    bars = []
    for scale, label in (("V", "抱石"), ("YDS", "绳索")):
        s = h.get(scale)
        if not s or not s["by_grade"]:
            continue
        items = sorted(s["by_grade"].items(), key=lambda kv: kv[0])
        top = max(n for _, n in items)
        segs = "".join(
            f'<div class="gbar"><div class="gfill" style="height:{100*n//top}%"></div>'
            f'<div class="gnum mono">{n}</div><div class="glbl mono">{esc(g)}</div></div>'
            for g, n in items)
        bars.append(f'<div class="gblock"><div class="glabel">{label}完攀分布</div>'
                    f'<div class="gbars">{segs}</div></div>')

    unknown = j["totals"].get("n_unknown_grade") or 0
    warn = (f'<p class="gwarn">有 {unknown} 条填了难度但认不出格式，未计入榜单。'
            f'抱石写成 V4，绳索写成 5.10b。</p>') if unknown else ""
    return (f'<div class="kpis">{"".join(cells)}</div>'
            f'<div class="gblocks">{"".join(bars)}</div>{warn}')


def mileage_row(j):
    """里程行：累计爬升 / 在墙时长 / 出手次数 / 爬岩天数。"""
    t = j["totals"]
    gain, gunit = fmt_gain(t.get("total_gain_bl"), j.get("height_m"))
    hours = round((t.get("total_climb_time_s") or 0) / 3600, 1)
    return ('<div class="kpis">'
            + kpi(gain, gunit, "累计爬升")
            + kpi(f"{hours}", "小时", "在墙时长")
            + kpi(f'{t.get("total_events") or 0}', "次", "累计出手")
            + kpi(f'{t.get("n_days") or 0}', "天", "爬岩天数")
            + '</div>')


def footprint_row(j):
    """足迹行：去过几个地点 + 连续周 + 动作收集。"""
    t = j["totals"]
    p = t.get("places") or {"n": 0, "list": []}
    cells = (kpi(f'{p["n"]}', "个", "去过的地点")
             + kpi(f'{t.get("streak_weeks_best") or 0}', "周", "最长连续")
             + kpi(f'{t.get("streak_weeks_current") or 0}', "周", "当前连续"))
    moves = t.get("moves_collect") or {}
    mcards = "".join(
        f'<div class="mcard"><div class="mname">{esc(m["name_zh"])}</div>'
        f'<div class="mcnt mono">×{m["n"]}</div>'
        f'<div class="mref mono">{esc(m.get("book_ref") or "")}</div></div>'
        for m in sorted(moves.values(), key=lambda x: -x["n"]))
    place_list = ('<p class="plist">' + esc(" · ".join(p["list"])) + '</p>') if p["list"] else ""
    return f'<div class="kpis">{cells}</div>{place_list}<div class="mcards">{mcards}</div>'
```

- [ ] **Step 3: 按月分组的目录**

在 `honor_row` 之前加：

```python
def catalog(entries, height_m):
    """按月分组、日期倒序的记录目录。"""
    by_month = {}
    for e in sorted(entries, key=lambda x: (x.get("date") or ""), reverse=True):
        by_month.setdefault((e.get("date") or "未知日期")[:7], []).append(e)
    out = []
    for month, es in by_month.items():
        rows = "".join(entry_row_static(e, height_m) for e in es)
        out.append(f'<div class="month"><div class="mhead mono">{esc(month)}'
                   f'<span class="mcount">{len(es)} 条</span></div>'
                   f'<div class="rows">{rows}</div></div>')
    return "".join(out)
```

- [ ] **Step 4: 记录行加类型与地点**

`entry_row_static` 里，把标签那一格（`<div class="c-tags">{grade}{sent}</div>`）改成同时显示类型：

```python
    typ = f'<span class="type">{esc(e["type"])}</span>' if e.get("type") else ""
    cells = (_row_head(e) + f'<div class="c-tags">{typ}{grade}{sent}</div>'
```

（`grade` 与 `sent` 两个变量的原有定义不动。）

- [ ] **Step 5: 拼进页面骨架**

`build_page` 里，把从 `<h1>攀岩账本</h1>` 到 `<div class="rows">{rows}</div>` 之间的
正文段整体替换为：

```python
<h1>攀岩账本</h1>
<div class="secrow">
  <section class="sec"><h2>荣誉</h2>{honor_html}</section>
  <section class="sec"><h2>里程</h2>{mileage_html}</section>
  <section class="sec"><h2>足迹</h2>{footprint_html}</section>
</div>
<h2>线路目录 <span class="mono" style="font-size:11px;color:var(--ink3)">{len(entries)} 条 · 新的在前</span></h2>
{catalog_html}
```

并在 `build_page` 里备好这几个变量：

```python
    honor_html = honor_row(j)
    mileage_html = mileage_row(j)
    footprint_html = footprint_row(j)
    catalog_html = catalog(entries, height_m)
```

- [ ] **Step 6: 补 CSS**

在 `climb_journal_card.py` 的 `<style>` 段末尾（`</style>` 之前）加：

```css
.secrow{display:grid;grid-template-columns:repeat(3,1fr);gap:18px;margin:18px 0 28px}
.sec{background:var(--card);border-radius:12px;padding:16px 18px;min-width:0}
.sec h2{margin:0 0 12px;font-size:12px;letter-spacing:.14em;color:var(--ink3)}
.sec .kpis{display:grid;grid-template-columns:repeat(2,1fr);gap:10px}
.gblocks{margin-top:14px}
.gblock{margin-top:10px}
.glabel{font-size:11px;color:var(--ink3);margin-bottom:6px}
.gbars{display:flex;gap:6px;align-items:flex-end;height:56px}
.gbar{flex:1;display:flex;flex-direction:column;justify-content:flex-end;align-items:center;height:100%}
.gfill{width:100%;background:var(--accent);border-radius:3px 3px 0 0;min-height:3px}
.gnum{font-size:11px;margin-top:2px}
.glbl{font-size:10px;color:var(--ink3)}
.gwarn{font-size:11px;color:var(--warn);margin:10px 0 0}
.plist{font-size:11px;color:var(--ink3);margin:10px 0 0}
.month{margin-bottom:22px}
.mhead{font-size:12px;color:var(--ink3);padding:0 0 8px;border-bottom:1px solid var(--line)}
.mcount{float:right}
.type{display:inline-block;padding:1px 7px;border-radius:9px;font-size:11px;
      background:var(--chip);color:var(--ink2);margin-right:5px}
@media(max-width:900px){.secrow{grid-template-columns:1fr}}
```

⚠️ 用到的 CSS 变量（`--card` `--accent` `--warn` `--chip` `--line` `--ink2` `--ink3`）
必须在文件已有的 `:root` 段里都存在。**先 grep 确认**，缺哪个就在 `:root` 里补一个
与现有配色协调的值：

```bash
grep -n "\-\-card\|--accent\|--warn\|--chip\|--line\|--ink2\|--ink3" climb_journal_card.py | head
```

- [ ] **Step 7: 删聚合层的旧字段**

`climb_journal.py` 里删掉 `grades` 函数、`totals` 中的 `"sent_by_grade": grades(entries),`
一行、以及 `tm["sent_by_grade"] = grades(month_entries)` 一行。荣誉数据现在全走 `honors`。

- [ ] **Step 8: 重新生成并用眼睛验收**

```bash
cd "C:/Users/Administrator/Documents/我的代码/climbing-video-mocap"
PYTHONUTF8=1 PYTHONIOENCODING=utf-8 /c/venvs/climb310/Scripts/python.exe climb_journal.py
PYTHONUTF8=1 PYTHONIOENCODING=utf-8 /c/venvs/climb310/Scripts/python.exe climb_journal_card.py
start 攀岩账本.html
```

检查清单：三行并列、目录按月分组、每条能点开报告卡、没有任何「填写」入口、
浏览器窗口拖窄到 800px 三行变竖排不塌。

⚠️ 此刻元数据仍全空，荣誉行应显示「—」而不是报错或 `None`。

- [ ] **Step 9: 跑测试**

```bash
PYTHONUTF8=1 PYTHONIOENCODING=utf-8 /c/venvs/climb310/Scripts/python.exe -m pytest tests/ -v
```

预期：`11 passed`

- [ ] **Step 10: 提交**

```bash
git add climb_journal_card.py climb_journal.py
git commit -m "feat(card): 账本首页重做，荣誉/里程/足迹三行并列 + 按月目录"
```

---

### Task 9: 报告卡返回页眉

详情页加一条回账本的路，并把这条线的难度/地点/完攀显示出来。**报告卡正文一个字不改。**

**Files:**
- Modify: `climb_report_card.py`（只动 `<div class="wrap">` 之后、`<h1>` 那一段）

- [ ] **Step 1: 加读 sidecar 的函数**

在 `climb_report_card.py` 的 `def main():` **之前**加：

```python
def sidecar_header(out_dir):
    """读 <素材文件夹>/线路.json → 返回页眉 HTML。没有 sidecar 就只给返回链接。"""
    import json as _json
    path = os.path.join(out_dir, "线路.json")
    chips = []
    if os.path.exists(path):
        try:
            with open(path, encoding="utf-8") as f:
                sc = _json.load(f)
        except (ValueError, OSError):
            sc = {}
        for key in ("类型", "难度", "地点", "线路名"):
            val = (sc.get(key) or "").strip()
            if val:
                chips.append(f'<span class="hchip">{val}</span>')
        if sc.get("完攀") is True:
            chips.append('<span class="hchip sent">完攀 ✓</span>')
        elif sc.get("完攀") is False:
            chips.append('<span class="hchip unsent">未完攀</span>')
    return ('<div class="cardhead">'
            '<a class="back" href="../../攀岩账本.html">← 返回账本</a>'
            f'<div class="hchips">{"".join(chips)}</div></div>')
```

返回链接是 `../../攀岩账本.html`：报告卡在 `素材/<片名>/`，账本在项目根，往上两级。

- [ ] **Step 2: 插进页面**

`climb_report_card.py` 里找到 `<div class="wrap">` 之后、`<h1>{A.base}</h1>` 之前，
插入 `{card_head}`，并在拼这段 HTML 的地方备好变量：

```python
    card_head = sidecar_header(args.out)
```

（`args.out` 就是报告卡的输出目录，即 `素材/<片名>/`。）

- [ ] **Step 3: 补 CSS**

在 `climb_report_card.py` 的 `<style>` 段末尾加：

```css
.cardhead{display:flex;align-items:center;gap:14px;flex-wrap:wrap;
          padding:0 0 14px;margin-bottom:10px;border-bottom:1px solid var(--line)}
.back{color:var(--ink3);text-decoration:none;font-size:13px}
.back:hover{color:var(--ink)}
.hchips{display:flex;gap:6px;flex-wrap:wrap}
.hchip{padding:2px 9px;border-radius:10px;font-size:12px;background:var(--chip);color:var(--ink2)}
.hchip.sent{background:var(--ok);color:#fff}
.hchip.unsent{color:var(--ink3)}
```

同样先 grep 确认 `--chip` `--ok` `--line` `--ink2` `--ink3` 在这个文件的 `:root` 里存在，
缺的补上（**这是另一个文件，变量表和账本页是分开的**）。

- [ ] **Step 4: 重新生成一张报告卡验收**

```bash
cd "C:/Users/Administrator/Documents/我的代码/climbing-video-mocap"
V=IMG_6152; D=素材/$V
PYTHONUTF8=1 PYTHONIOENCODING=utf-8 CLIMB_ROTATE=0 /c/venvs/climb310/Scripts/python.exe \
    climb_report_card.py --dir "$D/数据" --base $V --video "$D/$V.MOV" --out "$D"
start "$D/攀岩报告卡.html"
```

检查：顶部有「← 返回账本」，点它能回到账本页；元数据还没填所以没有 chip；正文与改动前一致。

- [ ] **Step 5: 提交**

```bash
git add climb_report_card.py
git commit -m "feat(card): 报告卡加返回账本页眉与线路信息"
```

---

### Task 10: 删旧填写服务 + 文档同步

**Files:**
- Delete: `climb_journal_edit.py`、`打开账本填写.bat`
- Modify: `README.md`、`CLAUDE.md`、`PLAN.md`、`CHANGELOG.md`

- [ ] **Step 1: 删文件**

```bash
cd "C:/Users/Administrator/Documents/我的代码/climbing-video-mocap"
git rm climb_journal_edit.py 打开账本填写.bat
```

- [ ] **Step 2: 确认没有残留引用**

```bash
grep -rn "journal_edit\|打开账本填写" --include=*.py --include=*.md --include=*.bat .
```

预期：只剩文档里待改的几处（下一步处理）。**任何 .py 里的命中都要清掉。**

- [ ] **Step 3: 更新 README**

`README.md` 的「看账本 / 填攀岩信息」整节替换为：

````markdown
### 看账本

双击 `攀岩账本.html` 就行，静态页，不用起任何服务。

### 分析一条新视频

1. 把视频丢进 `收件箱/`
2. 跟 Claude 说一句「分析」

Claude 会判重、跑流水线，**跑的同时在对话里问你**：类型（抱石/顶绳/先锋）、
难度、完攀与否、地点、（野外才问）线路名。答完就归档进 `素材/<片名>/`，
账本页自动刷新。

难度写法：抱石 `V4`，绳索 `5.10b`。两套刻度分开排榜，写混了会认不出来。

手动跑的话：

```bash
python3 climb_intake.py scan                    # 看收件箱有什么、哪些是重复的
python3 climb_intake.py run 收件箱/IMG_7001.MOV  # 跑六步流水线并落位
python3 climb_journal.py && python3 climb_journal_card.py   # 重算账本
```

远景素材（人在画面里很小）加 ROI 参数：

```bash
python3 climb_intake.py run 收件箱/xxx.MOV --roi --seed X,Y [--seed-roi W,H] [--start-s S]
```

`climb_intake.py` 已经把 Windows 三条铁律固化在脚本里，不用自己设环境变量。
````

并把「分析一条新线（五条命令一路到底）」那段代码块保留，但在它前面加一句：
「下面是流水线内部的六条命令，`climb_intake.py run` 就是按这个顺序跑的，
需要单独调某一步时才用得上。」——同时把 `climb_match.py` 补进那段命令清单，
它原先漏了（recognition.json 是动作收集的数据来源）：

```bash
python3 climb_match.py "$D/数据/${V}_segments.json"                            # → recognition.json
```

- [ ] **Step 4: 更新 CLAUDE.md 目录结构段**

把这三行：

```
climb_journal.py                           账本聚合层：扫 素材/*/数据/ + 线路.json → 素材/journal.json（--init 生成手填模板）
climb_journal_card.py                      账本展示层：读 journal.json → 根目录「攀岩账本.html」（J1 主干门面）
climb_journal_edit.py                      账本填写服务（127.0.0.1:8765）：serve 可编辑版账本页（总览+行内填写合一），保存写 线路.json+身高并自动重跑聚合；重复启动自动复用已有实例；老板双击 打开账本填写.bat
```

替换为：

```
climb_intake.py                            收件箱编排层：判重（视频指纹）+ 跑六步流水线 + 落位到 素材/<片名>/。三条铁律已固化，不算指标
climb_journal.py                           账本聚合层：扫 素材/*/数据/ + 线路.json → 素材/journal.json（含双刻度荣誉榜/地点足迹）
climb_journal_card.py                      账本展示层：读 journal.json → 根目录「攀岩账本.html」（三行并列 + 按月目录）
收件箱/                                     视频临时投放区，分析完自动搬进 素材/<片名>/
```

并在「硬约定」段末尾加一条：

```
- **元数据靠对话追问，不靠网页表单**（2026-07-20 定，实证）：J1 上过网页填写服务
  （climb_journal_edit.py + 打开账本填写.bat），7 条素材的 线路.json **一条都没填过**。
  改为 Claude 在跑流水线的同时于对话里追问五项（类型/难度/完攀/地点/线路名），
  写进 `素材/<片名>/线路.json`。**网页是输出，不是输入。** 填写服务已删除。
- **难度双刻度不能混榜**：抱石 V 级、绳索 YDS(5.x)，`climb_journal.parse_grade`
  负责解析。注意 **5.9 比 5.10a 简单**，字符串排序会搞反，必须用 rank。
  认不出格式的条目不进榜但仍算里程，账本页会如实标出有几条没认出来。
```

- [ ] **Step 5: 更新 PLAN.md 进度看板**

在 §10 末尾追加：

```markdown
**J2 录入流程重做（2026-07-20 完成）**：网页填写表单证伪——上线后 7 条素材元数据
一条没填。改为对话驱动：`收件箱/` 投放 → `climb_intake.py scan` 判重（视频指纹）→
`run` 跑六步流水线 → Claude 对话追问五项写 `线路.json` → 聚合刷新。账本页退回纯只读，
首页三行并列（荣誉/里程/足迹）+ 按月目录，报告卡加返回页眉收编为详情页。
删除 climb_journal_edit.py 与 打开账本填写.bat。新增难度双刻度解析（V / YDS）。
```

- [ ] **Step 6: 更新 CHANGELOG**

在 `CHANGELOG.md` 顶部加一条（沿用文件现有格式）：

```markdown
## 2026-07-20 — 账本录入改对话驱动

- 新增 `climb_intake.py`：视频指纹判重 + 六步流水线编排 + 落位，三条 Windows 铁律固化
- 新增 `收件箱/`：视频投放区，分析完自动归档进 `素材/<片名>/`
- `线路.json` 字段改版：岩馆→地点，新增类型/视频指纹/登记时间
- 账本聚合新增双刻度荣誉榜（抱石 V / 绳索 YDS）与地点足迹
- 账本首页重做：荣誉/里程/足迹三行并列 + 按月分组目录
- 报告卡加返回账本页眉（正文未改）
- **删除** `climb_journal_edit.py` 与 `打开账本填写.bat`——网页填写上线后零填写率
```

- [ ] **Step 7: 全量验证**

```bash
cd "C:/Users/Administrator/Documents/我的代码/climbing-video-mocap"
PYTHONUTF8=1 PYTHONIOENCODING=utf-8 /c/venvs/climb310/Scripts/python.exe -m pytest tests/ -v
PYTHONUTF8=1 PYTHONIOENCODING=utf-8 /c/venvs/climb310/Scripts/python.exe climb_journal.py
PYTHONUTF8=1 PYTHONIOENCODING=utf-8 /c/venvs/climb310/Scripts/python.exe climb_journal_card.py
PYTHONUTF8=1 PYTHONIOENCODING=utf-8 /c/venvs/climb310/Scripts/python.exe climb_intake.py scan
```

预期：测试全过；账本 7 条记录；scan 输出 `{"inbox": []}`。

- [ ] **Step 8: 提交**

```bash
git add -A
git commit -m "chore: 删除网页填写服务，文档同步对话驱动新流程"
```

---

## 收尾：老板验收

代码完成后由 Claude 与老板一起做，不属于自动化任务：

- [ ] **补齐 7 条旧素材元数据**：Claude 在对话里逐条问（类型/难度/完攀/地点/线路名），
      写进各自 `线路.json`，重跑聚合。这同时是追问流程的首次实测。
      注意六盘水斗牛是户外先锋 5.10b，其余 6 条是室内。
- [ ] **给 6 条旧素材补视频指纹**：`climb_intake.py run 素材/<片名>/<片名>.MOV` 会重跑
      整条流水线（慢）。更快的办法是只算指纹写进 sidecar：

```bash
PYTHONUTF8=1 PYTHONIOENCODING=utf-8 /c/venvs/climb310/Scripts/python.exe -c "
import json, os, climb_intake as ci
for name in sorted(os.listdir('素材')):
    d = os.path.join('素材', name)
    sc_path = os.path.join(d, '线路.json')
    if not os.path.isdir(d) or not os.path.exists(sc_path):
        continue
    v = [f for f in os.listdir(d) if f.lower().endswith(('.mov', '.mp4'))]
    if not v:
        continue
    sc = json.load(open(sc_path, encoding='utf-8'))
    if sc.get('视频指纹'):
        continue
    sc['视频指纹'] = ci.video_fingerprint(os.path.join(d, v[0]))
    json.dump(sc, open(sc_path, 'w', encoding='utf-8'), ensure_ascii=False, indent=2)
    print('补指纹', name)
"
```

- [ ] **端到端实测**：老板丢一个新视频进 `收件箱/`，说「分析」，走完全程。
- [ ] **判重实测**：把同一个视频改名后再丢一次，确认被拦下并报出原记录。

## 验收标准（对应 spec §验收标准）

1. 往 `收件箱/` 丢新视频说「分析」，全程对话完成录入，账本页出现新记录 → 收尾第 3 项
2. 同视频改名再丢，被判重拦下并报原记录信息 → 收尾第 4 项
3. 7 条旧素材补齐后首页数字与素材清单人工核对一致 → 收尾第 1 项
4. 首页每条可点进报告卡，报告卡可点回首页 → Task 8 Step 8 + Task 9 Step 4
5. 全程不需要启动任何本地服务 → Task 10 Step 1-2
