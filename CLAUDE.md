# CLAUDE.md — 攀岩视频动作分析项目

个人项目（Ned/老板的攀岩训练分析），非 EVYD 工作项目。

## 项目是什么

把单机位手机拍的攀岩视频变成一张动作分析报告卡：MediaPipe 骨架提取 → 指标计算 → HTML（对应 skill `ned-climbing-analysis`）。正在扩展三层新能力：**动作知识库**（从 PDF 教材构建）+ **动作识别**（规则匹配 → Claude 视觉判定 → 人工标注回流）+ **节奏/缺陷统计**（S3 已实现，待验收）。

⚠️ **v1 / v2a / v2b 只是开发阶段代号，不许出现在报告卡等用户可见的地方**（老板 2026-07-17：「我不明白 v1 v2a 都是什么」）。代码和 PLAN.md 里可以用。

## 开始工作前必读

1. **先读 [PLAN.md](PLAN.md)** —— 跨 session 的唯一计划真相：总体架构、全部设计决策、S1-S10 session 划分与进度看板。
2. 老板说「继续」「做 S{n}」时，按 PLAN.md 对应 session 的 目标/产出/验证 执行。
3. **每个 session 结束时更新 PLAN.md 进度看板**（状态/日期/验证结果），并 git commit。

## 硬约定

- `climb_pose.py`、`climb_analyze_report.py`（v1 流水线）**不要改**——保证历史视频结果可比。新能力全部走新文件。
- **指标与展示分层**（2026-07-17 定）：`climb_analyze_report.py` / `climb_segments.py` /
  `climb_report_v2.py` 各自只产出 `*.json`（metrics.json / segments.json / metrics_v2.json），
  它们是**产出契约**；`climb_report_card.py` 是唯一的展示层，读 json 出唯一那张
  「攀岩报告卡.html」。**改样式不碰指标，改指标不碰样式。** 老板 2026-07-17：
  「报告卡干嘛不合并」——v1/v2a 是开发阶段代号，不该漏到用户面前。
- **写图不能用 `cv2.imwrite`**：Windows 上遇到非 ASCII 路径**静默失败**（返回 False、
  不抛异常、不留文件；本项目路径含「我的代码」「素材」，必中）。v1 的
  climb_analyze_report.py 就栽在这——它的 5 张 crux 截图从来没落盘，报告卡里一直是裂图。
  用 `cv2.imencode` + Python `open(...,'wb')`，并检查返回值。**注意 `cv2.VideoCapture`
  读非 ASCII 路径是正常的**（走 FFmpeg 后端），只有 imwrite/imread 有这个坑。
- `<base>_recognition.json` 是动作序列的唯一真相文件，rule/vision/manual 三种来源逐级覆写，每段必须带 `source` + `confidence`。
- **素材里没有正例的判据不上线**（2026-07-18 定，两次实证）：阈值没有正例就无从标定，
  产出必是误报。GAP_PRESS_LO 只能取物理边界待正例精调；蹲跳/外撐识别试做后因零正例
  全是误报被砍（外撐 121 个全是宽站/踝认错，蹲跳抽验 3/3 假）。新判据上线前先问：
  这批素材里有没有它的真样本？没有就等素材，别硬造阈值。
- 长度/速度统一用 `body_scale`（肩中-髋中距中位数）归一化，与 v1 口径一致。
- 大文件（视频/PDF/report_assets 图）不入 git，见 .gitignore。
- **关节角度必须过两道独立体检才采信**（2026-07-17，详见 PLAN.md §8）：① 可见度（该肢体
  的两端关节都要看得见，VIS_MIN=0.3）；② **解剖学**（3D 重建的前臂/上臂比要落在 0.7-1.4，
  真人 ≈1.0）。**可见度分数预测不了重建质量**——实测 15-20% 的帧可见度达标却解剖学不可能
  （前臂算出来比上臂长 65%）。MediaPipe 对本项目素材的手臂 3D 重建只有 47-63% 的帧可信。
  另：**单目在 2D 上分不出「直臂」和「朝深度方向弯的臂」**（都投影成直线），这类争议靠几何
  判据裁决（直臂的上臂/前臂投影缩短比例应相同），不能靠看图。
- 教材 PDF 在 `knowledge_base/攀岩技術教本詳細圖解_抓撐轉跳我就是蜘蛛人.pdf`（老板 2026-07-17 放回本机）。不入 git（.gitignore 的 `*.pdf`）。kb_extract_pages.py 在 knowledge_base/ 和项目根目录两处都会找。
- 知识库动作分类沿用教材体系（5 大移动类 + 手顺 4 类），动作中文名用书中译名，记 `book_ref` 页码。
- **教材 PDF 页码换算：PDF 文件页 = 书本印刷页 + 2**（封面+版权页无编号，2026-07-15 实测校验）。book_ref 记的是印刷页。
- mediapipe 必须用 **0.10.14**（≥0.10.2x 移除了 `mp.solutions` 旧 API，v1 脚本会挂）。
- **Windows 上跑流水线的三条铁律（2026-07-16 首次在 Windows 跑通，逐条实测）**：
  1. **venv 必须建在纯 ASCII 路径**（本机 `C:\venvs\climb310`，Python 3.10）。项目路径含
     「我的代码」，mediapipe 无法从非 ASCII 路径加载模型，报 `FileNotFoundError:
     pose_landmark_cpu.binarypb`（文件其实存在）。项目代码留在原地没关系，只有 mediapipe
     包所在路径必须 ASCII。
  2. **跑 climb_pose.py 必须设 `CLIMB_ROTATE=0`**。OpenCV 在 Windows（FFmpeg 后端）默认
     自动应用旋转元数据（`CAP_PROP_ORIENTATION_AUTO=1`，4.10/5.0 都是），而脚本按 Mac
     行为（AVFoundation 后端不自动转）写了自己的手动转正 → **画面被转两次，人是躺着的**。
     后果极隐蔽：**检出率仍 100%**，但「重心高度」量的是水平方向，41s 的爬升会被算成
     2.6s 完攀。降 opencv 版本无用（4.10 也自动转）。数字荒谬时先怀疑上游数据方向。
  3. **设 `PYTHONUTF8=1 PYTHONIOENCODING=utf-8`**。v1 脚本的 `open(...,"w")` 没指定
     encoding，Windows 默认 GBK 写不了报告卡里的 `▸`；print 的 emoji 同理撞控制台 GBK。
  标准前缀：`PYTHONUTF8=1 PYTHONIOENCODING=utf-8 CLIMB_MAX_W=960 CLIMB_ROTATE=0
  /c/venvs/climb310/Scripts/python.exe climb_pose.py <视频> <输出目录>`
- **验证素材的隐性要求（S3 踩坑总结）**：① 全程不能有路人从镜头前走过——MediaPipe Pose
  只跟一个人，会跟错（已弃用的 IMG_6947 在 13-16s 就是，**检出率 98% 完全掩盖了这个问题**——
  检出率高 ≠ 跟对了人）；② 攀岩者不能爬出画面（IMG_6947 30s 后只剩腿在框内，重心用残缺
  骨架算）；③ 视频开头若含「走向岩壁」没关系，v2a 有上墙门会排除（见 PLAN.md §8）。
  **当前验证素材（2026-07-17 老板加了 5 条，全部跑通并逐帧核对过跟踪对象）**：

  | 片名 | 时长 | 类型 | 肘角有效帧 | 备注 |
  |---|---|---|---|---|
  | IMG_6152 | 31s | 竖拍抱石 | **76%** | 一上来就在墙上，数据最硬 |
  | IMG_6418 | 84s | 横拍抱石 | 72% | 见下「横移线」 |
  | IMG_6411 | 41s | 竖拍抱石 | 78% | 陡外倾壁，休息点误判样本 |
  | IMG_6417 | 69s | 横拍抱石 | 63% | 见下「横移线」 |
  | IMG_6321 | 66s | 室内顶绳 | 53% | 净上升 8.15bl / 51 段，接近竖直壁 |
  | IMG_6942 | 41s | 竖拍抱石 | 51% | 含走近段 |

  「肘角有效帧」= 至少一条臂同时过可见度和解剖学两道体检（口径见下方「关节角度」条）。
  - **IMG_6321 左下角有小孩全程站着，但没造成跟错**（t=2/8/32 逐帧核对，骨架一直在
    攀岩者身上）。它是「有第二个人但仍可用」的反例——判断得看标注视频，不能看检出率。
  - **IMG_6417 / IMG_6418 是横移线**（爬上去 → 横移 → 下爬），流水线按「一条线一次完攀」
    设计，只分析到最高点为止：6417 分析窗 0→14.2s / 视频 68.8s（**79% 在窗外**），
    6418 分析窗 0→27.4s / 视频 83.8s（67% 在窗外）。窗外是横移和下爬，不是漏掉的攀爬，
    但**报告卡不会告诉读者它只讲了 21% 的视频**。6417 的「顶点保持确认=False」也源于此
    （他没停在顶点，是横移走的）。两条视频开头人就在墙上，起攀判 0.0s 是对的。
  - **右臂有效帧率普遍远低于左臂**：IMG_6417 只有 3.1%、IMG_6418 4.5%、IMG_6152 62.1%。
    任何要求「两条臂都可信」的判据（比如把休息点改成两臂都直）都会先撞上这个天花板。
  - IMG_6947 因路人污染已被老板删除（2026-07-17），勿再引用。
  - **`素材/六盘水斗牛/`（28min，户外先锋 5.10b）：已用 `climb_pose_roi.py` 全量取数
    （2026-07-18，检出 99.8%，跟踪对象逐帧核对无误），真休息 4 个逐帧核对与画面吻合。
    远景素材必须走 ROI 版取数（标准前缀的 CLIMB_MAX_W=960 会让 MediaPipe 谁都检不出），
    播种参数与三态跟踪见脚本 --help 和 PLAN.md §9。28min 反复磨线，完攀/难点等聚合
    数字意义有限，用它主要验证休息点。**
  历史两条（IMG_6424/6952）2026-07-16 已整体清出（老板决定：可由视频重建，不留副本），
  旧 CSV 在 git 历史里，`git checkout 14b701f -- "IMG_6952_攀岩动作分析/数据/IMG_6952_pose2d.csv"`
  可取回。`annotations/` 的人工覆写与旧视频绑定，新视频需重新核对。
- **素材库结构（2026-07-17 老板定稿）：一条线一个文件夹 `素材/<片名>/`，原片 `<片名>.MOV`
  和全部分析产物（报告卡 / 数据/ / report_assets*/ / 标注视频/）都放在里面**，不再把视频和
  产物分开摆。整个 `素材/` 不入 git（原片大、产物可由原片复跑重建）。用法见 README。
- **MediaPipe 左右标签**：多数时候正确（IMG_6952 实测 12/15 段），但扭身/遮挡时会**整臂/整腿认错人**，且无启发式能可靠识别错误段（全局镜像互换、几何 x 判定、肩朝向、骨架链距离四种方案 2026-07-16 均实测否决——全局互换会把对的弄反）。定案：**标签默认可信 + 老板核对的错误段走 `annotations/<base>_side_overrides.json` 人工覆写**（source=manual），深度扭身段自动标 `side_confidence=low`。首尾 0.25s 速度有平滑边缘伪影，`climb_segments.py` 已钳制。
- moves.json 改动后跑 `kb_lint.py` 校验 + `kb_render.py` 重新生成 viewer。

## 目录结构

指标层各写各的 json，展示层只读 json（见「硬约定」的分层条款）：

```
climb_pose.py                              视频 → 3 份 CSV + 标注视频（v1，勿动）
climb_analyze_report.py                    → metrics.json（v1，勿动；HTML 是没用的副产品）
climb_segments.py                          → segments.json（S2 分段+换点事件，旋钮在顶部）
climb_report_v2.py                         → metrics_v2.json（S3 节奏六项+流畅度，旋钮在顶部）
climb_match.py                             → recognition.json（S4 窄版：仅位置/时序可判动作）
climb_keyframes.py                         卡点关键帧联络表 + 视觉判定协议（S5，协议在 docstring）
climb_pose_roi.py                          远景素材 ROI 跟踪取数（输出与 climb_pose 同格式）
climb_report_card.py                       ★展示层：读上面的 json → 唯一那张「攀岩报告卡.html」
climb_journal.py                           账本聚合层：扫 素材/*/数据/ + 线路.json → 素材/journal.json（--init 生成手填模板）
climb_journal_card.py                      账本展示层：读 journal.json → 根目录「攀岩账本.html」（J1 主干门面）
账本配置.json                               身高等全局配置（老板填一次，入 git）
climb_segments_review.py                   审阅视频生成（老板验收用，烧字幕）
PLAN.md                                    总计划 + 进度看板
素材/<片名>/                                原片 + 报告卡 + 数据/ + 标注视频/ + 线路.json（手填元数据，唯一入 git 的例外）
knowledge_base/                            moves.json + lint/render/extract 脚本 + moves_viewer.html（S1 产出）
annotations/                               人工左右覆写 JSON（不可再生，入 git）
_skill_stage/ned-climbing-analysis/        skill 暂存区（S10 收编时同步；归档约定已过时，见 PLAN.md）
FreeMoCap-Mac使用指南.md                    多机位 3D 方案资料（与本计划无关）

（`cases/` 标注案例库到 S6 才建，现在不存在。）

（教材 PDF 线下保管；历史视频分析目录 2026-07-16 已清出，见「验证素材」条款）
```

## 汇报口吻（对话汇报 + 报告卡文案通用）

完成后给老板：先结论、再 3-5 个关键数字、最后文件位置。不堆术语。

- **行文简洁，用词简单**（老板 2026-07-17 原话：「你的报告，行文能不能简洁，用词简单？」）。
  一句话说一件事，句子短，用常用词。少用「——」串句，不要为了气势加修辞。
  表格放数字，解释放表外。
  例：✗「先看 19–24 秒。这是你真正卡住的地方——不是拉不动，是不知道下一步怎么走。回看
  录像时重点想这里的解法。」 ✓「先看 19–24 秒。你卡在这里。不是拉不动，是没想好下一步。」
- **结论先行**：先给答案，再给数，定义/口径垫后。别让读者先背完定义才知道结论。
- 🚨 **不许发明老板没报告过的症状**。2026-07-17 我写过「你的停顿全是弯着胳膊找点，
  **这是前臂先泵的直接原因**」——老板从没说过他前臂泵，症状是我编的，因果是我倒推的。
  被当场顶回。**解读里每句话都要能指回一个具体数字或一张图**；指标只说指标测到的东西。
- **聚合判定 ≠ 逐个都成立**。「没有一次 X」不等于「每次都是非 X」。同一次顶回里，
  「没检测到直臂休息」是真的，但被我外推成「停顿全在弯着扛」——逐段看其实一半停顿
  手臂是直的（123-166°），只是不到 2 秒。要么逐段列，要么别概括。
- **数字必须描述读者眼睛看到的那一帧**。别用 ±N 帧窗口取极值去配一张单帧图（老板一眼
  看出「图里两手都伸直了，凭什么说肘屈到 92°」——那 92° 来自 3 帧之前）。
