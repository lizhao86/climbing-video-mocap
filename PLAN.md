# 攀岩动作识别 + 知识库 + 统计报告（总计划）

> 本文件是跨 session 的唯一计划真相。每个 session 执行前先读本文件，执行后更新「进度看板」。
> 批准时间：2026-07-15。原始计划：~/.claude/plans/pdf-pdf-validated-barto.md（以本文件为准）。

## 进度看板

| # | Session | 状态 | 完成日期 | 验证结果 |
|---|---|---|---|---|
| S1 | 知识库构建（精读 PDF 教材） | ✅ 完成 | 2026-07-15 | lint 通过（54 moves）；老板抽查待确认 |
| S2 | 全身动作分段 | ✅ 完成 | 2026-07-16 | 检测核心=锚点位移法 v2 + 末端换点语义（老板定义：手/脚净锚点位移≥0.5bl 才算真动作，膝动/拧身=调整不触发）；两层输出：move/static 段 + 肢体换点事件；IMG_6952 34 事件人眼零漏检、老板 3 处左右修正回流 annotations/；IMG_6424 零参数改动跑通（16 段/37 事件，结构自检合理），跨视频跨 fps 泛化 OK；审阅视频工具 climb_segments_review.py 随附 |
| S3 | 节奏统计（原「报告卡 v2a」） | 🔄 待老板验收 | 2026-07-17 | 六项指标全部实现（climb_report_v2.py → metrics_v2.json），IMG_6942 + IMG_6152 两条干净素材跑通并抽帧自验证。6942：起攀 1.7→完攀 31.4s，19-24s 卡住段由弯臂 2.0s + 同高度反复出手 + 4.58s 长停顿三信号互证，抽帧确认；6152：起攀 0.1→完攀 26.5s，9-17s 横跨 8.2s 只升 0.8bl 的卡住段正确捕获。~~遗留：第 6 项「休息点质量」从未被触发验证~~ → **2026-07-17 首次触发即误判（判据默认竖直壁），当天重做（见 §8）；2026-07-18 在六盘水 4 个真实挂绳休息上逐帧核对通过**（见下方计划外两行） |
| — | **合并报告卡**（计划外，老板 2026-07-17 提出） | 🔄 待老板验收 | 2026-07-17 | climb_report_card.py：一条线只出一张「攀岩报告卡.html」。起因老板两问：「我不明白 v1 v2a 都是什么」「报告卡干嘛不合并」。架构=指标与展示分层（见 §8）。暗色·影像优先，图表全内联 SVG，难点截图按骨架自动取景，时间线标记 + 弯臂段可悬浮看画面（热区按百分比定位，1440/885 两种宽度实测偏差 0px）。已砍重心轨迹图/四肢活动图（偏技术，对复盘无用） |
| — | **报告卡预览动图化**（计划外，老板 2026-07-17 提出） | 🔄 待老板验收 | 2026-07-17 | 静态预览图改 3 秒片段（切面前 1 后 2）悬浮播。老板要 gif，实测后改用 h264：同切片 gif 2.6MB/10fps 带噪点 vs mp4 90KB/30fps 全彩，一份报告 1-2.3MB 与纯静态图同量级。挖出两坑：素材是 VFR，ffmpeg -ss 走真实 PTS 与 v1 名义时间轴漂 53-105ms（3 帧），改 cv2 喂帧；移开鼠标停回切面帧而非片头，图文才对得上 |
| — | **休息点质量判据重做**（计划外，B 阶段） | ✅ 完成 | 2026-07-18 | 三病根：直臂既是门槛又加分（满分是结构必然）/ 只看较直臂 / FOOT_HIGH 缺下界（gap=-0.21 拿「压脚+」）。修法见 §8 与 docs/superpowers/specs/2026-07-17-rest-quality-redesign.md。IMG_6411 100→60；六盘水 4 个真休息（质量 70/30/55/75）逐帧核对与画面全部吻合，GAP_PRESS_LO=0 获正例回看无调整依据。A 阶段（错过的休息机会+甩手检测）未启动 |
| — | **六盘水远景取数**（计划外，climb_pose_roi.py） | ✅ 完成 | 2026-07-18 | 三态 ROI 跟踪（窄框播种→垂直逃逸→标准框），全片 44134 帧检出 99.8%、跟踪对象逐帧核对无误。评估+取数细节见 §9。教训：belayer 贴着攀岩者时，检出率 100% 三次都没暴露跟错人，全靠标注视频人眼核对 |
| — | **指标价值 review + A/B/C 三路线**（2026-07-18 老板发起「结论不精准/意义不大」的全面 review） | ✅ 完成 | 2026-07-18 | 深度调研(102 子任务对抗验证)+内部自查 → docs/2026-07-18-指标价值review与路线决策.md,老板拍板 A→B→C。**A 流畅度**:GIE+归一化jerk 进 metrics_v2(只用2D髋轨迹,GIE与卡点独立互证);报告卡瘦身(KPI=完攀/净上升/轨迹熵/卡住N处;砍姿态极端+攀爬效率;停顿口径改中性)。**B 动作窄启动**:climb_match.py 识别 6 个位置/时序可判动作→recognition.json;抽帧核对 high_step/match 全对,cross 两验一假定死 low;报告卡「动作记录」区块只陈列 conf≥medium。**C 卡点视觉判定**:climb_keyframes.py 出关键帧联络表+判定协议;5 个卡点看图判定(source=vision 注入 recognition.json),报告卡卡点卡片带「看过画面」标记;**实证价值:纠正一处几何误报**(IMG_6411@24.8「反复试探」实为连贯掛腳序列) |
| S4 | 特征+规则匹配 | ✅ 完成（窄版+验收达标） | 2026-07-18 | 原计划 54 动作全量**被决策文档否决**（2/3 在单目不可信信号区）。终版 climb_match.py：high_step/match/hand_change 上卡（抽验 **6/6 全对，达 ≥80% 标准**）+ cross/heel_hook 记 low 不上卡 + 手顺统计。**试做后砍掉两个**：外撐（121 个全是宽站/踝认错）、蹲跳（抽验 3/3 假——逮到的是快速换手和远景噪声）。铁律沉淀：**素材里没有正例的动作不上规则**，阈值无从标定必出误报。另修：识别加攀爬窗过滤（起攀前走动曾产出假动作）。角度类动作永久走视觉判定 |
| S5 | 视觉判定环节 | ✅ 首轮交付（卡点范围） | 2026-07-18 | climb_keyframes.py + 协议（写在脚本 docstring）。只扫卡点不扫全程（几何当筛选器）。中置信全程扫描如需要再议 |
| — | **价值重定位：旅程账本为主干**（2026-07-18 老板发起从头脑暴） | ✅ 方向定案 | 2026-07-18 | 结论见 §10。主干从「单线评估」换成「旅程账本」（Strava 模式），报告卡收编为详情页；MediaPipe 定位修正为计量器+粗筛器，不再当裁判。S6-S9 随之暂缓 |
| J1 | 旅程账本首版（聚合层+账本页） | 🔄 待老板验收 | 2026-07-18 | climb_journal.py（聚合）+ climb_journal_card.py（展示）→ 根目录「攀岩账本.html」。7 条素材全扫出，聚合数字与逐条手算核对一致（Σ爬升 45.00bl / 在墙 30分15秒 / 出手 751 次 / 5 个攀爬日）；日期全部读到真实拍摄时间（ffprobe quicktime tag，mtime 是拷贝时间不可靠）；6 条可点进报告卡，六盘水如实标「未生成报告卡」。设计定案见 docs/superpowers/plans/2026-07-18-journey-ledger.md。**待老板**：7 份 素材/*/线路.json 填难度/完攀/岩馆，账本配置.json 填身高（填完重跑两脚本，出「V5 完攀 ×N」和米数） |
| S6 | 标注界面+回流 | ⏸ 暂缓 | | 2026-07-18 价值重定位：服务「把骨架裁判养准」路线，已降级，等真实需求再启动（§10） |
| S7 | 案例参考生效（kNN + few-shot） | ⏸ 暂缓 | | 同 S6，依赖 S6 且服务已降级路线 |
| S8 | 报告卡 v2b（动作序列/构成/缺陷） | ⏸ 不再单独做 | | 报告卡「动作记录」区块已覆盖核心；剩余并入账本「动作收集类」（§10） |
| S9 | 手型视觉识别（按需启动） | ⏸ 暂缓 | | 原本就按需，价值重定位后更靠后 |
| S10 | skill 收编 | ⬜ 待做 | | 保留，时点顺延到账本层落地后一并收编 |

状态图例：⬜ 待做 / 🔄 进行中 / ✅ 完成 / ⏸ 暂缓

依赖图：S1（第一个执行，老板拍板）；S2→S3；(S1+S2)→S4→{S5,S6}→S7→S8；S9 按需；S10 收尾。
**2026-07-18 起主线转向：J1 旅程账本（§10）优先，S6-S9 暂缓，评估层维持现有战果不扩张。**

---

## Context

在现有攀岩视频分析流水线（ned-climbing-analysis skill：MediaPipe 骨架提取 → 指标计算 → HTML 报告卡）之上，加三层能力：**动作知识库**（从老板提供的 PDF 教材构建）、**动作识别**（混合路线：骨架规则匹配 → Claude 视觉判定 → 人工标注回流成案例库，越用越准）、**统计报告**（完成速度、动作序列、切换耗时、难点、动作缺陷、休息质量）。

老板已拍板的决策：
- 节奏统计和动作识别**都要，分阶段实现**
- 识别**以身体姿势为主**，手型（crimp/gaston 等）能看就看，看不到之后再做（阶段三）
- 混合识别路线：规则 + 视觉 + 人工标注回流
- **分多个独立 session 执行**，每个 session 有明确输入/产出/验证，用现有 IMG_6952 视频做验证素材
- **S1 知识库先行**

知识库素材：`攀岩技術教本詳細圖解_抓撐轉跳我就是蜘蛛人.pdf`（東秀磯著，161 页，项目根目录）。评估结论：物理原理驱动 + 带量化图解，是理想素材。其分类体系直接沿用为知识库分类：**5 大移动类**（调节平衡/吊挂/推拉/反作用力/动态移动）+ **手顺 4 类**（左右轮流/bump/换手/交叉）+ 手型 13 种（Ch2，阶段三用）+ 脚法踩法（Ch2）。

## 总体架构

```
climb_pose.py ──► CSV ──► climb_segments.py ──► <base>_segments.json
                              │
knowledge_base/moves.json ────┤
cases/cases.json ─────────────┼─► climb_match.py ──► <base>_recognition.json
                              │        │ (中置信度)
                              │        ▼
                              │   climb_keyframes.py ──► 关键帧 → Claude 会话内 Read 看图判定
                              │        │ (仍不确定)
                              │        ▼
                              │   climb_annotate_html.py ──► 标注界面.html → 老板标 → 导出 JSON
                              │        ▼
                              │   climb_merge_annotations.py ──► 更新 recognition.json + cases.json
                              ▼
                    climb_report_v2.py ──► 新版报告卡.html + metrics_v2.json
```

原则：每步幂等、可单独重跑；`recognition.json` 是唯一动作序列真相文件，被 rule/vision/manual 三种来源逐级覆写，每段记 `source` + `confidence`；v1 脚本（climb_pose.py / climb_analyze_report.py）**不动**，保证历史两条视频可比。

## 关键设计决策

### 1. 知识库 schema（knowledge_base/moves.json）

单 JSON 文件，每个动作：`id / name_zh(用书中译名) / aliases / category(沿用书的分类体系) / kind(posture静态|action动态) / book_ref(章节页码) / description / when_to_use / detectability(rule|rule_weak|vision|manual) / pose_rules / visual_cues_for_claude / common_errors / confusable_with`。

- **特征 = 命名特征 + 梯形隶属度区间**：condition 写 `{feature, range:[lo,hi], soft, weight, gate?}`，落区间得 1 分、soft 缓冲带线性衰减。读 PDF 只产出「哪个特征、什么区间、多重要」，规则引擎通用，加新动作零代码。
- `gate:true`（如可见度）不过 → confidence=null（遮挡≠不是该动作）。
- `side:either` 左右镜像各评一次取高者。
- 长度统一用现有 `body_scale`（肩中-髋中距中位数）归一化，与现有代码口径一致。
- posture 类带 `min_hold_s`（最短持续时长）。
- 教材定性为主：量化阈值由 Claude 定初值（书中物理讲解和图解做依据，note 记出处），再用老板视频调参 + 标注回流校准。**预期对齐：知识库是"读书起步、靠标注养准"。**

### 2. 动作分段（climb_segments.py）—— 2026-07-16 检测核心改为锚点位移法 v2

原计划的「速度滞回阈值法」（allspeed=max(四肢速度) + mean±kσ 滞回）**已实测否决**：
对慢速换点有结构性盲区——IMG_6952 的 14-20s 慢速横移整段漏检，速度可以一直低于
任何合理阈值，但位移必然累积。与全程 3fps 抽帧人眼直读对照后换方案。

现行方案（锚点位移法）：
- 每关节（四肢端点+双膝）在 0.5s 滑窗内位移 > D_MOVE=0.3 身长 → 该帧"换点中"
- **两层输出**：① timeline 段（六关节并集→合并/去短→move/static，给节奏统计）；
  ② **肢体换点事件**（每一手/每一步：limb+时间段+位移向量，S4 动作识别的基本单元）
- 事件过滤对人眼时间轴 18 窗口扫参定值（EV_MIN_DEV=0.4bl、EV_MIN_DUR=0.3s、同肢体间隙 0.5s 合并）：40 事件零漏检
- 左右判定：MediaPipe 标签默认可信（12/15 段正确），扭身/遮挡的错误段无启发式可救
  （镜像互换/几何位置/肩朝向/链距离四方案均实测否决），走 `annotations/<base>_side_overrides.json`
  人工覆写（source=manual），深度扭身段自动标 side_confidence=low
- 首尾 0.25s 速度边缘伪影钳制（平滑+梯度在边界造出假 19bl/s）
- **static 段同样输出**——posture 类动作（heel hook、锁定、休息、旗式）发生在静止段，是识别主战场
- 旧 count_moves 口径保留在 metrics.json 不动
- 审阅工具：climb_segments_review.py 生成烧字幕审阅视频（实时显示"出X肢"+✎人工/？低置信）

### 3. 特征注册表（features.py）

帧级（posture 用，左右成对、身长归一化、y 向上为正）：`ankle_rel_hip_y、knee_rel_hip_y、wrist_rel_shoulder_y、knee/elbow/hip_angle、heel_leads_toe_x、foot_pitch、ankle_cross_midline、feet_spread、hands_gap、wrists_crossed、shoulder_width_ratio（侧身深度代理）、hip_wall_proxy、torso_lean、*_visibility`。
段级（action 用）：主导肢体、位移方向幅度、peak_com_speed、双手同时脱点、段前后髋高变化、段时长。
注：pose2d.csv 已含 heel / foot_index 点，挂脚/toe hook 判定可用。

### 4. 2D 可判性结论（写进知识库 detectability 字段）

- **rule 可判**：高跨步、挂脚 heel hook、直臂悬挂、锁定 lock-off、蹲跳 lunge/dyno、同点 match、交叉手、外撑 stemming、rockover、抖手休息
- **rule_weak 需视觉确认**：挂旗 flag（内/外旗区分靠视觉）、垂膝 drop knee、侧身/twist-lock、toe hook、下壓 mantling、deadpoint
- **vision/manual（阶段三）**：手型 13 种（无手指关键点）、smearing、锁膝 knee bar、campus

### 5. 置信度分层（阈值放 climb_match.py 顶部，验证时用 IMG_6952 调参）

top1 分数 s1、与 top2 差距 margin：
- **高**（s1≥0.75 且 margin≥0.20 且 rule 类）→ 直接采纳 `source:"rule"`
- **中**（0.45≤s1<0.75 或 margin<0.20 或 rule_weak/vision 类）→ 截 3 帧（段起/峰值/段末，原视频帧 + 骨架对照版）→ Claude 会话内 Read 判定，prompt 附规则命中明细 + visual_cues + 案例库 few-shot 图
- **视觉后仍中** 且段时长≥1s 或近 crux → 进人工标注队列
- **低**（s1<0.45 视觉也无候选）→ move 段标 `basic_move`、static 段标 `rest_or_adjust`，不打满屏 unknown

### 6. 标注 HTML（climb_annotate_html.py 生成）

单文件自包含，关键帧 base64 内嵌（每段 3 帧 480px JPEG）。每段一屏：3 帧可放大、Top-3 候选按钮（含分数+命中条件中文解释）、「其它动作…/普通移动/跳过」、备注框；快捷键 1/2/3/0；localStorage 防丢；底部「导出标注」Blob 下载 `<base>_annotations.json`（兜底：复制剪贴板）。老板导出后放 `annotations/` 目录，跑 merge 脚本回流。

### 6.5 已否决方案：教材卡通图跑骨架建 pattern（2026-07-15 实测否决）

MediaPipe 在教材手绘插图上实测：7 个用例（整页+单人裁剪）仅 2 个检出且关节点明显错位（髋部飘到背部等），单人裁剪图反而全部检不出。结论：**量化 pattern 只能来自真实视频的标注回流（见 7. 案例库），书图只做人看 + Claude 视觉判定的参考图**。不要再提议在卡通图上提取骨架特征。

### 7. 案例库（cases/cases.json + cases/keyframes/）

跨视频累积。每条案例：label、side、source(manual>vision)、时间段、**全量特征向量快照**、关键帧路径、老板备注。
- **kNN 校正（规则层自动）**：z-score 欧氏距离 k=3；近邻一致 → 置信 +0.15（封顶 0.95）；与规则 top1 冲突且距离近 → 强制进视觉裁决；案例 <5 条静默不生效
- **few-shot（视觉层）**：视觉判定时附 1-2 张最近邻已确认关键帧

### 8. 新版报告卡（climb_report_v2.py，v1 保留，分两步交付）

**v2a 节奏统计（不依赖识别，S3 交付）**：
- 完成时间：起攀（COM 首次持续上升 >0.3 身长）→ 完攀（COM 达全局最高后保持 >1.5s）
  - **2026-07-16 补「上墙门」**：原规则在含"走向岩壁"的视频上必然把起攀判到 t=0。
    相机静止，人走向岩壁=在**深度方向**远离相机=画面里变小且上移，2D 重心高度被透视
    被动抬高。IMG_6942 实测：走近的 2s 里 COM"上升"1.09bl（0.55bl/s），真正爬墙 25s
    才升 2.5bl（0.1bl/s）——**走路比爬墙"升"得还快**，任何基于速率/位移的规则都分不开，
    全局最低点必然落在走路起点。解法：先用**深度无关**的身体自身相对量
    （同一深度两点之比不受距离影响）判断是否已搭上岩点——较高那只手腕高过肩线且持续
    0.5s——再从该时刻起套用 COM 规则。IMG_6942 起攀 0.0→1.7s（画面 1.5-1.6s 搭点，吻合）；
    IMG_6947 判 0.0s（该视频一开始人就在墙上，正确）。
    ⚠️ 已否决：用每帧表观肩髋距探测走近阶段——被躯干旋转的 ±20% 波动淹没，实测不可用。
- 切换耗时：每 move 段前 static 段时长 = 准备/犹豫时间，条形图 + 中位数
- 难点：准备时长 >中位数×2.5，或同高度区间反复 move ≥3 次；与 v1 crux 互证，双命中标「确认难点」
- 休息/攀爬占比：static 再分 rest（直臂+低速+>2s）/ adjust，三段占比
- 弯臂检测：静止段肘角持续 <150° 超 2s = 弯臂耗力（高置信硬指标，先只上这类）
- 休息点质量：直臂(+)、脚高髋低(+)、难点前(+)、弯臂(−)、过长>8s(−)
  - 🚨 **2026-07-17：该项的几何判据默认「墙是竖直的」，在外倾壁上会把发力判成休息。**
    IMG_6411（新素材）首次触发这项：13.83-16.78s 报「真休息、质量 100 分」。抽帧核对
    发现他挂在陡的外倾壁（purple prow）上，两手都抓点，右臂 154-160°、**左臂 134-143°
    是弯的**，且 2.94s 里右膝从 131.6° 伸到 155.7°——在蹬腿换位置，不是挂着不动。
    三条加分依据里两条站不住：① 「直臂挂着」只取了**张得更开的那条臂**（elbow_open_med
    =154.9°），没管另一条弯着；② 「脚高髋低、重心压脚」在外倾壁上恰恰意味着**核心正
    绷紧**，与休息相反（竖直壁上才等价于把重量压给骨骼）。
    对照组支持：IMG_6321（接近竖直的顶绳壁）最长停顿 8.76s 都没被判成休息，竖直壁上
    它反而保守。抱石馆陡壁占多数 → 误判不会是孤例。
    **✅ 已修（2026-07-17 当天，B 阶段，设计见 docs/superpowers/specs/
    2026-07-17-rest-quality-redesign.md）**。老板拍板：该指标只评价「已发生的停顿休得
    好不好」，不判断「错过的休息机会」（那是 A 阶段：需要「手脱开甩手」检测，甩手与
    伸手够点是同一信号靠方向区分，风险大，单独一轮再做）。三处修法：
    ① 基础分 60→75，删掉「直臂+15」——直臂是准入门槛，凡休息必满足，再加分是假分辨率
    （旧版满分是结构必然）；② 压脚加分改**区间** `0 ≤ gap < 0.80`（GAP_PRESS_LO 旋钮），
    gap<0（脚高过髋，重量物理上压不到脚）另扣 10——教材锚点：外側掛旗法「把腰大幅沉
    下来休息」p.82-84，休息=沉腰，与脚过髋的挂姿相反；锁膝是例外但「机会不多」且
    rule_weak，不为它弯曲主判据；③ 另一条**可见**（过两道体检）的臂中位肘角 <150° 扣
    20，不可见不扣（存疑从宽），JSON 新增 left/right_elbow_med、other_arm_bent、
    steep_hang。**没改准入门槛**——IMG_6411 那类停顿仍算休息，只是分低（方案 2，
    老板选定）。验证：IMG_6411 100→60，reasons=难点前(+)/左臂弯 140°(−)/脚过髋(−)，
    全部指回数字；其余五条 metrics 零变化（diff 过）。
    ⚠️ GAP_PRESS_LO=0 取的是物理硬边界不是数据拟合——全部素材只有 1 个误判负例、
    0 个真休息正例。等六盘水取数后用真样本回来精调。

**2026-07-16 S3 实现要点（写死在 climb_report_v2.py 顶部旋钮，验收后可调）**：
- **可见度门槛必须按左右臂各自独立判定，且要低（VIS_MIN=0.3）**。攀岩时手抓在岩点上
  贴着墙，MediaPipe 给手腕的 visibility 长期很低（实测中位数 0.19-0.38）。
  用"左右肘+左右腕四点同时 >0.5"的与门只剩 **9%** 的帧可用，rest/弯臂必然全灭；
  改单臂门槛 0.3 后"至少一条臂可用"回到 **77%**。肘角取自 world landmarks，
  已抽帧人眼核对低可见度下依然准（3.8s 直臂 167°、4.2s 弯臂 94° 与画面一致）。
- **可见度闪断要补洞**（VIS_GAP_FILL_S=0.5）：0.2s 的瞬时闪断会把连续弯臂段切碎导致
  达不到 2s 阈值而漏检（实测 4.6s 的弯臂段被切成 2.1s）。
- FOOT_HIGH=0.80：髋-踝落差中位数实测 0.9-1.0bl，取 25 分位才是"明显比平时踩得高"。
- **🚨 v1 的 crux（吃力点）实测退化成了「关节弯得深」排序，不是「用力」排序**
  （2026-07-17 拆解，起因：老板看着 14.4s 那张卡说「图里人明明在放松，不剧烈」）。
  v1 原式：`强度 = z(重心加速度) + z(肢体速度) + (120-膝角)/40 + (120-肘角)/40`。
  IMG_6942 全片四项的实际分布：

  | 项 | 中位数 | 90 分位 | 最大 |
  |---|---|---|---|
  | z加速度 | **-0.12** | **0.04** | 28.55 |
  | z速度 | **-0.26** | 0.73 | 13.70 |
  | 膝屈项 | 0.48 | 1.44 | 2.08 |
  | 肘屈项 | 0.61 | 1.43 | 2.69 |

  两个 z 分数被极端离群值撑大的标准差压扁，正常帧几乎恒为 0；**屈曲项才是实际主导**。
  实测 5 个 crux 的主导项：3.1s=加速度(62%)、7.3s=膝屈(52%)、9.8s=膝屈(66%)、
  14.4s=速度(47%，且总分最低)、37.6s=肘屈(63%，还在完攀之后)。
  **攀岩时膝盖弯 78° 完全可以是舒服地踩在脚点上**——把屈曲等同于发力在本领域不成立。
  处理：报告卡不再声称「动作剧烈」，改为如实报出每处的主导项（见 climb_report_card.py）。
  ⚠️ **S8「动作缺陷」若打算复用 v1 的 crux，先读这条**；真要「发力」信号需另设计
  （建议：加速度用分位数而非 z 分数，避免被离群值压扁）+ 抽帧验证。v1 脚本仍冻结不改。
- **❌「双命中=确认难点」已废除，改为两类难点并列展示**（老板 2026-07-17 拍板）。
  IMG_6942 实测双命中 **0**，抽帧核对确认 **v2a 判对了**（19-24s 人确实卡在同一高度弯臂
  硬扛），v1 五个吃力点无一命中该段。原因：两者测的是**正交的现象**——
  · **v1「发力型」**：重心加速度 + 肢体速度 + 关节极端屈曲 → 找"拼尽全力"的瞬间；
  · **v2a「卡住型」**：停顿异常长 / 同高度反复出手 → 找"不知道怎么办"的时刻。
  真正的难点常常是后者，而 v1 结构上看不见它。硬套互证只会把真难点标成"疑似"。
  现在两类各自标 source（`v2a` / `v1`）并列列出，`v1_nearby_t` 仅记录时间邻近关系供参考、
  不影响可信度。实测两种情况都存在：IMG_6152 的 12-13s 卡住型与 v1 的 13.4s 邻近（既难想
  又难拉），IMG_6942 的两类则完全错开（费力的地方和想不明白的地方是分开的）。
- **「同高度反复出手」必须加最小横跨时长**（REPEAT_MIN_SPAN_S=4.0）：不加会在**起攀处
  必然误报**——起步几手的中点高度天然都落在最低那个箱里。IMG_6152 实测：起步 0.23-3.23s
  出手 3 次但 3s 爬了 0.67bl（正常起步，滤掉）；9.2-17.37s 出手 3 次却 8.2s 只升 0.8bl
  （真卡住，保留）。detail 里同时报出"横跨 Xs 却只上升 Ybl"，让判据自解释。
- **顶点保持要报实际时长而非只报 True/False**：IMG_6152 保持 1.37s vs 阈值 1.5s，其实是
  正常完攀后下撤，报 False 会误导成"可能没到顶"。现在 metrics_v2.json 出 `top_hold_s`，
  报告卡按"≥60% 阈值"给不同措辞。
- **🚨 肘角必须过两道独立体检：可见度 + 解剖学**（ANAT_LO/HI=0.7/1.4，2026-07-17 加）。
  起因：老板说「14.4s 图里右臂看着挺直的」。查下去发现三件事：
  1. **单目在 2D 上分不出"直臂"和"朝深度方向弯的臂"**——直臂投影是直线，朝相机弯的臂
     投影也是直线。14.4s 右臂 2D 量角 170.7°（看着直）、3D 量角 111.6°（弯）。
     判据：**直臂的上臂与前臂朝同一 3D 方向，投影缩短比例相同，2D 的前臂/上臂比应 ≈1.0**；
     实测 0.50 → 两段朝向不同 → 肘部确实有弯，只是弯向深度方向。老板的眼睛和 2D 数据
     面对同一张投影，有同样的盲区——**这类争议不能靠看图裁决，要靠几何**。
  2. **MediaPipe 的 3D 重建约一半时间在解剖学上就是不可能的**：前臂/上臂比在
     IMG_6942/6152 上只有 47-63% 的帧落在 0.7-1.4，95 分位达 2.0-2.9（前臂比上臂长 2-3 倍）。
  3. **可见度分数预测不了重建质量**：15-20% 的帧可见度达标却解剖学不可能。两者独立，必须
     都过。实测这道门槛把一个被烂重建撑到 149°（差点够到直臂线 150° 被判成"休息"）的
     静止段打回 123° 真值；弯臂段 19.3-22.8s 则从 3.41s 收紧到 2.04s，结论存活。
  报告卡须把「两道体检都过的帧占比」露出来——它决定了弯臂/休息这两项结论有多硬。
- **rest 分支在三条素材上全部未触发**，但**「从不直臂休息 → 停顿全在弯着扛」是过度推断，
  已废除**（老板 2026-07-17 顶回）。逐段看是**分裂**的：IMG_6942 有 3 次停顿明确弯着
  （81-99°），另 4 次手臂接近伸直（123-143°）；IMG_6152 甚至有停顿达 166°（完全直臂），
  只是都短于 REST_MIN_S=2s 而没被计为 rest。**成立的只有「没有检测到 ≥150° 持续 2 秒的
  直臂休息」这一句**，不能外推成"全程弯着扛"。
  ⚠️ 更严重的是当时还写了「这是**前臂先泵**的直接原因」——**老板从没说过他前臂泵，这是
  凭空捏造的症状 + 倒推的因果**。教训：指标只说指标测到的东西，不要发明用户没报告过的
  现象来给结论加戏。
  副作用：休息点质量（第 6 项）实现了但未被任何素材验证，需一条含真实休息的视频。
  注：抱石线路短而连续，真休息本就是先锋/长线才有的东西，若后续素材仍全是抱石，此项
  可能长期无法验证——届时考虑放宽 REST_MIN_S 或改口径。

**报告卡合并（climb_report_card.py，2026-07-17 老板拍板）**：
一条线只出**一张**「攀岩报告卡.html」，v1/v2a 是开发阶段代号，不该漏到用户面前。
- 架构：v1/v2a 的脚本都不改——`metrics.json` / `metrics_v2.json` 就是它们的产出契约，
  报告卡只做展示层，读两份 metrics + segments + CSV + 原片。
- v1 的 HTML 是冻结脚本的必然副产品，`--out` 指到 `数据/v1原始/` 眼不见为净。
- 设计：暗色·影像优先。图表全部**内联 SVG**（可主题化 / 任意缩放清晰 / 与 HTML 精确对齐 /
  彻底摆脱 matplotlib 的中文字体依赖）；难点截图按骨架包围盒**自动取景**成 4:5 特写；
  时间线的三角标记与弯臂段**可悬浮浮出当时画面**（纯 CSS，热区按百分比定位——
  实测 1440/885 两种宽度下与 SVG 图元偏差均为 0px；**用 px 会错位**，SVG 有缩放系数）。
- 已砍：重心轨迹图、四肢活动图（偏技术，对复盘无用）。
- ⚠️ **写图不能用 `cv2.imwrite`**：Windows 上遇到非 ASCII 路径**静默失败**（返回 False、
  不抛异常、不留文件）。v1 的 climb_analyze_report.py 就栽在这——它的 5 张 crux 截图
  从来没落盘，报告卡里一直是裂图。改用 `cv2.imencode` + Python `open()`，且写不成就抛异常。

**v2b 动作识别增强（S8 交付）**：
- 动作序列：泳道时间轴（按书的 5 大类着色，static 灰）+ 明细表（时间/动作/来源/置信度）
- 注：S8 若要沿用「v1 互证」思路，先看 §8 v2a 里「双命中已废除」那条——v1 的 crux 与
  节奏异常正交，不能互相当验证。
- 动作构成：各类型计数图（分类沿用书的体系）
- 动作缺陷：知识库 common_errors 逐段跑（挂点过低等），附时间+截帧。只上线高置信少数几条，避免误报

## 9. 户外远景素材（六盘水斗牛 5.10b）—— 2026-07-17 可行性评估

`素材/六盘水斗牛/`：28min、4K 竖版（cv2 读到 2160x3840）、52.27fps（VFR，标称 60）、11.9GB。
户外先锋，机位固定，belayer 全程在画面底部。28 分钟里反复上下磨同一条线（有坠落、有
挂绳休息）——**这是目前唯一可能含真实休息的素材**，抱石/顶绳线短而连续，真休息本就是
先锋才有的。

**现流水线为什么跑不了**：标准前缀的 `CLIMB_MAX_W=960` 把 2160 宽压到 960，攀岩者从
4K 下的 ~360px 高被压到 ~160px。实测全画幅→960 宽喂 MediaPipe：**检出=否**（连
belayer 都没检出——所以失败模式不是「跟错人」，是「谁也检不出」，我先前的推测是错的）。

**ROI 跟踪实测可行**（roi_probe：人工播种一个 ROI 中心，每帧按上一帧骨架包围盒重新居中）：
- **连续 10 分钟 / 1200 帧（675→1275s），检出率 100%**，跟踪没丢，无需重播种。
- **跟的确实是攀岩者，不是 belayer**（⚠️ 必须单独验，检出率证明不了这个——IMG_6947
  就是 98% 检出却跟着路人）：ROI 中心 y 全程落在 1591-1964，belayer 站在 y≈2900-3300，
  从没靠近；相邻采样点（2s 间隔）最大跳变仅 103px，无瞬移；11:15-20:15 每分钟抽一帧
  画骨架人眼核对，10/10 都在攀岩者身上。
- **ROI 尺寸对结果几乎没影响** —— 宽 520（放大 1.85x）→ 肘角 38.8%/7.5%；
  宽 760（1.26x）→ 38.1%/7.5%；宽 1100（0.87x）→ 41.8%/4.3%。
  **瓶颈不是分辨率**，是这个机位拍不到他右臂（人正对岩壁、右半身背镜头且被身体/岩石挡）。
  裁得再紧也变不出不存在的信息 → 不需要做自适应变焦，固定尺寸跟踪 ROI 就够。
  **通用判据**：远景素材扫 2-3 档 ROI 尺寸，若结果不随尺寸改善，就是视角问题，
  任何后处理都白费，只能换机位重拍。
- **belayer 跳跃风险实测不成立**：28 个采样点（每 60s）他每一帧都在墙上，全程没回过地面
  —— 整段是挂绳休息而非放下来重爬。所以不必加「只认上墙的人」的门。

**肘角有效帧 ~40%**（90s 窗口：左 38.1% / 右 7.5%；10min 长跑：左 41.2% / 右 15.4%），
与其他素材同尺子对比（可见度+解剖学两道门）：

| 素材 | 左肘 | 右肘 | 至少一条臂 |
|---|---|---|---|
| IMG_6152 | 21.7% | 62.1% | 76.2% |
| IMG_6418 | 70.6% | 4.5% | 71.6% |
| IMG_6417 | 62.2% | 3.1% | 62.8% |
| IMG_6321 | 32.9% | 23.3% | 53.0% |
| IMG_6942（在用） | 35.0% | 23.9% | 50.9% |
| **六盘水（ROI）** | **38.1%** | **7.5%** | **~40%** |

（此表复现了本文档既有的 IMG_6942=51% / IMG_6152=76%，口径一致。）
六盘水是六条里最差，但**与已在用的 IMG_6942 同量级，不是废**；右臂 7.5% 也不是它独有的
毛病（6417 才 3.1%、6418 才 4.5%）。

⚠️ **可见度与解剖学两道门高度独立，必须取 AND**：实测 675s 左肘可见度 0.99 而解剖学
1.65（不可能）；750s 右肘可见度 0.03 而解剖学「合格」0.74——**后者更危险**，可见度接近 0
时 MediaPipe 是在凭先验猜一条解剖学合理的胳膊，合格是假象。

**✅ 2026-07-18 已全量取数并验证（climb_pose_roi.py，三态跟踪）**：
- 全片 2→1690.7s，44134 帧（STEP=2，有效 26.1fps），检出 99.8%，全画幅重扫仅 1 次
  （1594.4s，瞬时丢检原地找回，前后中心只移 9px，不是跳人）。
- **跟踪对象全程正确**：ROI 中心 y 范围 751~2753，唯一接近 belayer 区（≈2800-3300）的
  时段就是起步 2.0-11.7s（攀岩者本人在低处）；相邻最大跳变 92px 无瞬移；
  起步/中段/重扫点/片尾 6 帧人眼核对全部在攀岩者身上。
- **起步锁定的三次试错**（都记在 climb_pose_roi.py 的 commit 里，教训通用）：
  belayer 与攀岩者起步相距仅 175px → ① 标准 ROI 首帧就抓 belayer（检出率 100%
  完全不暴露）；② 窄框播种但 t=0 攀岩者弯腰检不出 → 重扫按「最近」又选中 belayer；
  ③ 锁定后立刻恢复标准框 → belayer 回框、tracking 因画面剧变重选人。
  定案三态：窄框等首检出（禁重扫）→ 锁定后仍窄框直到**垂直逃逸** → 标准框+重扫。
  播种要挑攀岩者**站直**的时刻（--start-s 2）。
- **真休息 4 个，均分 57.5，逐帧核对全部与画面吻合**（36-42s 质量 70 右臂直左臂 80° 弯；
  771-783s 质量 30 挂 12s 偏久；1171-1177s 质量 55；1350-1354s 质量 75 素基准分）——
  重做后的判据首次在真实先锋休息上工作正常，逐臂扣分与画面一致。
- **GAP_PRESS_LO 正例回看**：4 个真休息 gap=1.19~1.43，全部在 FOOT_HIGH=0.80 之上
  （竖直壁站姿休息，脚远低于髋）；唯一负例 IMG_6411 gap=-0.21。正负例之间隔着
  (-0.2, 0.8) 的巨大空隙且区间内暂无样本 → **下界 0 维持，无调整依据**。
  「压脚 +10」在这批样本上一次都没触发——它本来就是给板墙/高脚点休息设计的，合理。
- 已知限制：28min 反复磨线与「一条线一次完攀」假设不符，完攀 1634.9s / 卡住型 87 处
  这类聚合数字意义有限（同横移线限制）；rest 占比 1.6% 明显偏低，46.45s 的最长停顿
  没被判休息（多半是挂绳晃动破了低速门或直臂门），后续分析这条素材时再查。
  合并报告卡需要 v1 metrics.json（老流程），本次只取数未生成报告卡。

原评估（2026-07-17，实测依据保留如下）：需要新建 `climb_pose_roi.py`（不动
climb_pose.py，见硬约定）：
① 人工播种首帧 ROI 中心（或用首帧全画幅多尺度扫一遍自动找）；② 固定尺寸 ROI 跟踪 +
每帧重新居中；③ 把 ROI 内的归一化坐标换算回全画幅坐标再写 pose2d.csv，让下游
（segments / report_v2 / report_card）零改动直接用。下游对齐要点：`body_scale` 天然免疫
（同一深度两点之比），但 **ROI 在动，所以重心高度必须换算回全画幅坐标系**，否则人不动而
ROI 动会造出假位移。另：本条素材 VFR 52.27fps，切片/抽帧走 cv2 名义时间轴（见硬约定）。

**剩余顾虑（值得先跟老板对齐再动手）**：这条素材的价值全押在「验证休息点质量」上，
而该项的判据本身正待重做（见 §8 休息点质量下的 2026-07-17 条）。**先修判据、还是先做
ROI 取数**——建议先修判据：判据没定之前取来的数没法判断对错。

## 10. 价值重定位：旅程账本为主干（2026-07-18 脑暴定案，老板逐项拍板）

起因：老板发起从头脑暴——项目价值到底是什么？并质疑 MediaPipe 流水线是否合适。
完整定案存 `~/.claude/plans/1-2-mellow-tome.md`，要点如下。

**① 对「实现方式好不好」的回答：流水线没错，错的是「让它当评估的主判官」。**
一年战绩：打得赢的都是「几何粗筛 + 看画面裁决」（卡住型难点、C 路线视觉纠错）；
翻车的都是「骨架数字直接下结论」（54 动作砍到 6、蹲跳/外撐全误报、肘角半数帧可信、
单目分不清直臂与深度弯臂）。定位修正：MediaPipe 当**计量器**（爬升/时间/次数，误差
互相抵消，精度绰绰有余）和**粗筛器**称职，当**裁判**不行，不再往裁判方向加投入。

**② 主干 = 旅程账本（Strava 模式）。** 四个消费场景全要（当天复盘/长期积累/分享
炫耀/精彩瞬间），但门面是「你的攀岩人生」：累计数字 + 进步曲线；每条线是账本一条
记录，点进去才是现有报告卡（收编为详情页）；分享卡从账本一键生成；集锦独立模块
与指标解耦（事件时间点当剪辑候选，剪辑走 ned-vlog-producer 思路）。

**③ 门面数字四类全要，按成本排序**：打卡类（爬馆次数/本月条数/连续周数，扫现有
json 白送）→ 积累类（累计爬升米数=身长×老板身高换算、在墙总时间、累计出手次数，
白送）→ 动作收集类（S4 的 6 个动作当收集品累计，数量统计对误差宽容）→ 进步类
（同线多次尝试对比，等素材出现同线重复爬）。

**④ 手动元数据：老板愿意每条视频花几秒填**难度等级+完攀与否+岩馆名（sidecar
`素材/<片名>/线路.json`）。换来「本月 3 条 V5」这种硬荣誉数字——攀岩圈的荣誉货币
是难度不是米数。

**架构（J1 已实现，2026-07-18）**：`climb_journal.py`（聚合层）扫 `素材/*/数据/
*_metrics_v2.json` + 手填 sidecar `素材/<片名>/线路.json`（中文键：线路名/岩馆/难度/
完攀/日期/备注，`--init` 生成模板，入 git）→ `素材/journal.json`；
`climb_journal_card.py`（展示层）只读 journal.json → 根目录「攀岩账本.html」。
身高放根目录 `账本配置.json`（填一次，null 时爬升显示身长）。日期优先级：手填 >
ffprobe quicktime.creationdate（实测全 7 条都读得到真实拍摄日期）> creation_time
> mtime（mtime 是拷贝时间，三条素材撞同一分钟，别信）。动作收集与报告卡同口径
（conf≥medium，即 high_step/match/hand_change 三种；cross/heel_hook 定死 low 不进）。
连续周数出两口径：最长连续（荣誉数字）+ 当前连续（本周为锚，断了就是 0，不粉饰）。
符合分层硬约定：指标层出 json、展示层只读 json；v1 不动；流水线原样保留。

**J1 首版验收标准（预告）**：扫全部现有素材文件夹能出一张账本页，打卡/积累两类
数字与素材清单人工核对一致，每条记录可点进对应报告卡。

## 分阶段 Session 详情

### 阶段一：知识库 + 节奏统计

| # | Session | 产出 | 验证 | 依赖 |
|---|---|---|---|---|
| S1 | 知识库构建（精读 PDF 教材 Ch3 移动手法脚法 + Ch2 脚法） | knowledge_base/moves.json（姿势类动作，含 book_ref）、kb_lint.py | lint 通过；老板抽查 5 个动作与书对照 | 无 |
| S2 | 全身动作分段 | climb_segments.py、IMG_6952_segments.json、段边界叠加图 | 人眼对照标注视频核对 10 段边界；move 段数落在 12-18 合理区间 | 无 |
| S3 | 节奏统计报告 v2a | climb_report_v2.py（v2a 部分）、IMG_6952 新报告卡 + metrics_v2.json | 老板验收：完成时间/切换耗时/难点/休息占比/弯臂检测与视频观感一致 | S2 |

### 阶段二：动作识别（姿势类）

| # | Session | 产出 | 验证 | 依赖 |
|---|---|---|---|---|
| S4 | 特征+规则匹配 | features.py、climb_match.py、IMG_6952_recognition.json | 抽 8 段核对 top1；高置信准确率 ≥80%；记录调参 | S1+S2 |
| S5 | 视觉判定环节 | climb_keyframes.py、视觉判定协议（写 SKILL.md）、更新 recognition.json | IMG_6952 中置信段实跑一轮 | S4 |
| S6 | 标注界面+回流 | climb_annotate_html.py、climb_merge_annotations.py、标注界面.html、cases.json 首批 | 老板实标一轮→导出→merge 往返测试 | S4 |
| S7 | 案例参考生效 | climb_match.py 加 kNN；视觉 prompt 加 few-shot | 重跑 IMG_6952：已标类型置信提升、人工队列变短 | S6 |
| S8 | 报告卡 v2b（动作序列/构成/缺陷） | climb_report_v2.py 完整版 | 老板验收动作序列与视频一致 | S4（S5-S7 更佳但非必需） |

### 阶段三：手型与增强（后做，按需启动）

| # | Session | 产出 | 说明 |
|---|---|---|---|
| S9 | 手型视觉识别 | 知识库补 Ch2 手型 13 种（纯 vision 类）；关键帧手部放大裁剪；视觉判定协议扩展 | 无骨架特征可用，纯 Claude 看图；先验证单目画质下手型能否看清再决定投入 |
| S10 | skill 收编 | 更新 SKILL.md + scripts/ 同步全部新脚本 | 按 SKILL.md 对 IMG_6952 从零完整重跑全流程 |

## 文件清单

新建（项目根目录）：`knowledge_base/moves.json`、`knowledge_base/kb_lint.py`、`features.py`、`climb_segments.py`、`climb_match.py`、`climb_keyframes.py`、`climb_annotate_html.py`、`climb_merge_annotations.py`、`climb_report_v2.py`、`cases/`、`annotations/`

修改：`_skill_stage/ned-climbing-analysis/SKILL.md` + `scripts/`（S10 时同步）

不动：`climb_pose.py`、`climb_analyze_report.py`

## 关键参照文件

- `climb_analyze_report.py` — 特征/尺度口径、count_moves、crux 逻辑复用来源
- `climb_pose.py` — CSV 列定义与 33 点命名
- `素材/IMG_6942/数据/`、`素材/IMG_6152/数据/` — 当前验证素材（pose2d/angles/landmarks CSV
  + metrics.json + metrics_v2.json + segments.json）。旧的 IMG_6952/6424 已清出、IMG_6947
  因污染被删（2026-07-17），勿再引用
- `knowledge_base/攀岩技術教本詳細圖解_抓撐轉跳我就是蜘蛛人.pdf` — 知识库素材
  （S1 精读 Ch3 + Ch2 脚法；S9 读 Ch2 手型）。注：kb_extract_pages.py 需要它在**项目根目录**
- `_skill_stage/ned-climbing-analysis/SKILL.md` — S10 要更新的工作流文档。⚠️ 里面的归档约定
  （`<片名>_攀岩动作分析/` 与视频分开摆）已过时，S10 同步时要改成 2026-07-17 定稿的
  `素材/<片名>/`（原片与产物同folder），并补 Windows 三条铁律
