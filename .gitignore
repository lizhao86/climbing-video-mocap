# 大文件不入库
*.mov
*.mp4
*.MOV
*.MP4
*.pdf

# 系统与缓存
.DS_Store
__pycache__/
*.pyc

# 素材库：一条线一个文件夹，原片 + 全部分析产物都在里面（2026-07-17 结构定稿）。
# 整体不入 git——原片是大文件，产物均可由原片复跑重建（2026-07-16 老板决定不留副本）。
# 例外：线路.json 是老板手填元数据（难度/完攀/岩馆），不可再生，入 git（2026-07-18 账本层）。
素材/**
!素材/*/
!素材/*/线路.json

# 账本页与聚合产物可由 climb_journal*.py 重建，不入 git
攀岩账本.html

# 兼容旧结构（2026-07-17 前的散放目录）
*_攀岩动作分析/
*/report_assets/
*/report_assets_v2/
cases/keyframes/*.jpg
cases/keyframes/*.png

# 虚拟环境
.venv*/

# 教材页图（版权物+可由 kb_extract_pages.py 重新生成）
knowledge_base/book_pages/

# 收件箱：临时投放区，视频不入 git，目录本身保留
收件箱/*
!收件箱/.gitkeep
